{
  "name": "orq-fixture",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Repo mínimo que o kit instancia para exercitar o motor ponta a ponta. Não é biblioteca: existe só para atravessar o loop.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.20.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
