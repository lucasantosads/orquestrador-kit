# PLAYBOOK do Orquestrador — seed

> Memória viva do loop. **Todo incidente vira uma linha aqui, no mesmo commit que o corrige.**
> As lições abaixo já foram pagas com incidente real no repo de origem — este repo começa com elas, não do zero.

## Princípios
- **Código verifica, modelo julga.** Aprovação vem de comando executável; a opinião do modelo sobre o próprio trabalho não conta.
- **Amnésia por design.** Cada braço reconstrói o estado da fonte única (fila + git); nada de estado implícito entre rodadas.
- **Fail-fast por custo.** Cheque barato antes de gasto caro, sempre.
- **A fronteira é código.** Allowlist + guards físicos; prompt é desejo, enforcement é lei.
- **Fonte única de estado.** Fila/status/evidência num único checkout.

## Lições de campo (herdadas)
- **Faixa livre no disco ≠ faixa livre no ledger.** Antes de reservar faixa de migrations/IDs, checar reservas DOCUMENTADAS (CLAUDE.md, ledger, docs de arquitetura), não só `ls` no diretório. Reserva documentada de módulo futuro prevalece sobre disco vazio.
- **Pasta existente ≠ spec presente.** Um `mkdir` sobrevive a um `unzip` falho; preflight e pré-voo verificam CONTEÚDO dos arquivos (`test -s`, contagem em `git ls-files`), nunca só o diretório.
- **Headless não enxerga skill de chat.** `claude -p` via launchd/cron/CI só lê o que está no repo — a doutrina TEM que estar vendorizada em `docs/orquestrador/skill/` e o prompt do executor referencia a cópia local. Instalação que dependia de skill externa rodou sem spec.
- **Log ≠ verdade; git é o ground truth.** Um "aprovado" no log era outro ticket rodando de casa paralela.
- **Executor da casa errada cria ESTADO PARALELO** — dois estados que parecem válidos. Preflight de identidade é obrigatório e fatal.
- **Lock robusto = pid + idade máxima + cleanup em TODO caminho de saída** + auto-limpeza de órfão no início.
- **Deps referenciam o campo `id` do JSON, nunca o nome do arquivo.**
- **Campo fora do schema é ignorado em silêncio** — valide nomes contra o schema.
- **`tail -f` prende o terminal** — leitura pontual quando for encadear comandos.
- **Gerador de story ≠ gerador de ticket.** O gate é critério executável + verificação de fonte. "Existe no brief" ≠ existe no disco: todo nome de tabela é verificado no repo, inclusive em brief humano.
- **Nunca reinício forçado do serviço com run vivo** — checar lock/pid antes; reinício por cima = dois executores na mesma fila.
- **Parser do veredicto: falha de parse = adiado, nunca reprovação.** Incidente real: juiz aprovou, parser declarou ilegível, trabalho bom foi descartado. Preserve sempre o output CRU do juiz.
- **Branch de ticket bloqueado não é deletada** até revisão humana — o commit é evidência recuperável (sha fica em runs/, mas branch viva é mais barata de auditar).
- **Writeback externo é upsert de seção única**, nunca append (duplica a cada sync).
- **Relatório com credencial ausente cai para arquivo local** — falha silenciosa esconde o placar.
- **Allowlists sobrepostas entre tickets pendentes exigem dep explícita** — senão o segundo nasce de base velha e o merge conflita no registry compartilhado.
- **Critério de UI cobra integração, não existência** — componente criado mas nunca importado/montado passa em "arquivo existe" e falha em produção; o juiz deve cobrar o import/mount.
- **Merge manual guiado: resolução completa ANTES de qualquer add/commit** — placeholder no meio do fluxo mandou markers de conflito pro origin.
- **Build ≠ typecheck** em setups com `ignoreBuildErrors` — o gate real é o typecheck explícito contra baseline.

## Registro
- **2026-09-01** — Skill v2 vendorizada em `docs/orquestrador/skill/` (SKILL.md, CHANGELOG-v2.md, 8 references, 10 templates). Config migrado para `$schema_versao: 2` de forma **aditiva**: 233 caminhos do config anterior conferidos um a um, nenhum valor alterado além do próprio `$schema_versao`. Sentinela e planejador nascem **desligados** (`loops.sentinela.ativo=false`, `loops.planejador.ativo=false` em `dry_run`); só o executor fica ligado. **Comportamento do executor inalterado** — `executor.sh` não foi tocado e segue lendo os mesmos campos v1. `EXECUTOR.md` presente (29 linhas) mas **ainda não injetado no prompt**: o executor continua com o ponteiro para `SKILL.md`; a injeção é da Etapa 3.
- **2026-09-02** — Fase 0 brownfield: MAPA v2 criado (`docs/roadmap/`), 5 blocos com faixas disjuntas (B0 000-099 `concluida`, B1 100-199 motor honesto, B2 200-299 demo readiness `em_andamento`, B3 300-399 piloto pago, B4 400-499 loop de aprendizado), 17 frentes, 79 tickets estimados, 8 gates humanos como token (nenhum em `liberacoes.json`). Frentes `pronta`: só B2-F1 (Gate 205) e B2-F2 (health+smoke). ADRs 0001-0005 gravados. Docs da Fase 0: `brief.md`, `prd.md`, `arquitetura.md`; `PRD.md`/`architecture.md`/`product-brief.md` movidos para `docs/legado/` com cabeçalho (a migration 0001 que os cita **não** foi tocada: aplicada é imutável). `CLAUDE.md` corrigido — o gate 205 é de dois estágios, não uma linha em `textoDoRoteiro`. Tickets 200-202 escritos com **teste vermelho verificado à mão**, porque `orq gate` ainda não existe: cada `cmd` alvo rodado contra o checkout, conferindo rc e saída.
  - **Preflight de processo passa a ser escopado por repo**, não `grep` global: `cwd` do processo em `~/Projetos/conteudos-infinitos` ou lock em `docs/fila/runs/`. Dois orquestradores na mesma máquina são operação normal — o `grep -E 'local-loop|claude -p'` global pegou o loop do comarka-operacional e teria abortado a sessão sem motivo.
  - **Critério com `$(...)` sobre arquivo que ainda não existe falha com rc 2, não com veredito.** `test $(wc -l < arquivo-ausente) -ge 10` vira "unknown condition" — erro de shell, indistinguível de bug do gate. Guarde a existência primeiro: `test -s arquivo && test $(grep -c '' arquivo) -ge 10 && echo ok`.
  - **`espera` é igualdade exata.** "nao-zero" não existe como valor esperado; a forma que funciona é `test $(grep -c ...) -ge 1 && echo ok` com `espera: "ok"`.
  - **O que o ticket NÃO pode tocar fica FORA da allowlist, não num critério.** `pos-aprovacao.ts` saiu da allowlist da frente B2-F1 de propósito: o enforcement reprova o diff antes do juiz, e isso é mais forte que qualquer guarda.
  - **Zona proibida corta escopo de ticket.** `.env*` é `no_write_paths`, então "env de exemplo" não podia entrar no 202; a variável nova é documentada e a linha do `.env.example` é acrescentada por humano.
  - **Colisão de teste, custo real (201).** O 201 reprovou 2x por colisão de teste: o critério exigia remover o checkbox de compliance e a suíte guiada afirmava a regra antiga fora da allowlist; o agente não tem saída nesse caso — a tentativa que editou o teste dependente ficou 887p/0f e 7/7 critérios, e caiu no enforcement com `fora_do_pathspec`; as duas que respeitaram a fronteira ficaram vermelhas no gate. **Regra: critério de remoção/contagem 0 ⇒ grep do símbolo em `*.test.*` fora da allowlist antes de entrar na fila.** Harness v1 não sinaliza `fora_do_pathspec` no retry (remédio 2 da doutrina, Etapa 3).
  - **Colisão não é só de teste — é de qualquer arquivo obrigatório fora da allowlist.** No mesmo 201, `grep -c 'aplicarCompliance' WizardGerar.tsx` não podia chegar a 0 porque `apps/web/src/app/gerar/actions.ts` declarava o campo como obrigatório na Server Action e estava fora da allowlist. Ao escrever critério de contagem 0 sobre um símbolo, siga também os CHAMADORES do arquivo, não só os testes.
- **2026-09-03** — Drenagem do lote 2 promovida em `main` (201, 203, 204, 205; B2-F1 fecha com 200-205 done). Lote 3 escrito: 206 (B2-F2), 207-210 (B2-F3, ativação persistente).
  - **`202` aprovado com teste que chama o handler direto: a rota existia mas era inalcançável sem sessão.** `apps/web/middleware.ts` tem matcher que **não** exclui `/api`, e `src/lib/supabase/middleware.ts:42` redireciona para `/login` toda rota fora de `PUBLIC_PREFIXES` — o smoke da staging nunca veria o `200`. O teste do 202 chamava `GET()` em processo, que roda **depois** do middleware e por isso não prova nada sobre a requisição real. **Regra: ticket que cria rota ou tela declara a classe de auth (pública ou protegida) e prova alcançabilidade — via `isPublicPath` ou teste que atravessa o middleware; teste de handler não substitui.**
  - **O lint manual do mapa tinha duas regras que a doutrina não tem, corrigidas hoje.** (1) *Teto de 8 paths na allowlist da frente*: 8 é `gate_ticket.max_paths_allowlist`, que vale por **ticket**; a allowlist de uma frente é a **união** das dos seus tickets e é naturalmente maior. Virou aviso acima de 16, nunca falha. (2) *Gate liberado sai do mapa*: `gates_humanos` é o que o bloco **exige**, `liberacoes.json` é o que já foi **satisfeito** — o token estar nos dois é gate cumprido, não vazamento. O check passa a falhar só quando um ticket **aberto** depende de `humano:<token>` que nenhum bloco declara. `scripts/roadmap/lint-mapa.py` é a semente do `orq mapa lint`.
  - **`liberacoes.json` está num formato que o loop não lê.** O arquivo traz `{"tokens": ["humano:migration-0025", ...]}`; `lib.sh:liberacao_ok` consulta `jq '.liberadas[] | select(.token == $t)'` com o token **sem** o prefixo `humano:`. Nenhum token liberado hoje resolveria uma dependência `humano:*` — o ticket ficaria pendente para sempre, em silêncio. O lint do mapa passou a emitir `WARN` sobre isso; a correção do arquivo (ou de `liberacao_ok`) é decisão humana pendente. Por isso os tickets 207-210 **não** declaram `humano:migration-0025`: a 0025 foi confirmada aplicada lendo o banco. **Leitura de `information_schema`/`pg_catalog` só em sessão interativa, com humano presente — o loop nunca lê nem escreve no banco**, em nenhum papel (executor, juiz, sentinela): `no_write_tables: "TODAS"` continua valendo e a leitura fica fora do braço automático, senão o gate passaria a depender de estado que não está no repo.
  - **Preflight de processo por `cwd` provou-se de novo.** Três `local-loop.sh` vivos na máquina, todos com `cwd` em `comarka-operacional` (`lsof -a -p <pid> -d cwd`). Um `grep` global teria abortado a sessão à toa.
---
- **2026-09-04 (Etapa 3, sessão C)** — O ticket 227 e as duas peças que ele pagou, em branch dedicada (`harness/etapa3-c`), fora do loop. A sessão inteira nasceu de UM ticket, e vale registrar por quê: **227 foi a primeira vez que o juiz reprovou com razão** — e foi aí que se descobriu que o harness não sabia o que fazer com uma reprovação legítima.
  - **A lição do 227, em uma frase: o veredito estava certo e tudo o que veio depois dele estava errado.** A tentativa 1 produziu 178 linhas, passou todos os gates mecânicos (`typecheck=ok testes=ok enforcement=ok criterios=15/15 build=ok`) e o juiz reprovou por um achado real — o teste ponta a ponta afirmava só `ok === false` e a AUSÊNCIA da palavra "onboarding", sem nenhuma asserção positiva de que o erro devolvido era o de `indisponivel`; passaria igual com o código antigo. Exatamente a classe de coisa que gate mecânico não pega e que motivou criar o juiz. **Aí o retry recriou a worktree do zero**, o agente abriu uma árvore vazia, não achou nada do que o diagnóstico mandava corrigir, e devolveu **diff 0 em 15 s e 3 s** — duas tentativas queimadas em 18 segundos, `BLOQUEADO` por `criterio_qualidade`, um ticket cujo trabalho estava a uma correção de distância. **Reprovação boa virou bloqueio injusto porque o remédio era aplicado numa árvore onde a doença não existia.** Corrigido pela peça 0 (retry na mesma worktree quando o enforcement passou).
  - **Peça 0b · o requeue sobrescreveu a evidência da rodada que ele veio consertar.** Ao voltar o 227 para `pendente`, a drenagem seguinte recomeçou o contador de retry em 0 — correto, retry é por rodada — e nomeou o diretório de evidência com esse mesmo número. Resultado em `runs/227/attempt-0/`: `prompt.txt` (20:15) e `claude.txt` (20:16) **de hoje** ao lado de `gates.txt`, `criterios.txt` e `juiz.veredito.json` **de ontem**. O prompt e a saída da tentativa que gerou o diff de 178 linhas — a evidência da única reprovação legítima que este loop já produziu — foram **destruídos e não têm cópia**. Contador de retry e slot de evidência viraram duas coisas: `slot_base_attempt` lê o disco e a numeração continua (`attempt-3`, `attempt-4`), e `meta.json` ganhou `dir` para amarrar um ao outro. **Regra: número que reinicia nunca nomeia arquivo que persiste.**
  - **Peça 0b · trabalho verificado não se perde por falta de `git commit`.** Na mesma rodada o agente gastou 22 turnos e US$ 0,52, escreveu **150 linhas** em `org.ts`, `personas.ts` e um teste novo — e saiu sem commitar. O pipeline mede `git diff base...HEAD`, então viu **diff 0**, e a worktree seria removida com tudo dentro. Agora `commit_do_agente` commita por ele (`wip(<id>): agente saiu sem commitar`, com trailer `Orq-Ticket`) antes de medir o diff, e a tentativa segue para enforcement, gates e juiz como qualquer outra. **Não é indulto: é o contrário.** O `git add -A` do harness — proibido ao agente, permitido aqui porque o enforcement roda logo depois e vê tudo — inclui de propósito o arquivo fora da allowlist, que antes passava batido com diff 0 e agora é reprovado. O juiz recebe nota de que o commit foi do harness (campo `nota` de `EntradaPrompt`), para não julgar o diff pela mensagem de commit.
  - **O que matou o executor daquela rodada continua sem dono, e a instrumentação para descobrir não existia.** Prova do que dá para afirmar: não há linha `custo:` no log e não há registro `executor/227` no `custo.json` para aquela chamada, embora `custo_registrar` sempre logue e sempre devolva 0 — testado com o envelope real. Entre `claude_run` retornar e `custo_registrar` não há nada mais, e a chamada inteira roda sob `|| rc=$?`, que **desliga o `errexit`**. Logo não foi `set -e`: foi **sinal**. Quem mandou não dá para saber: o run foi manual (sem linha do `launchd-run.sh`), o executor não tem `trap`, e `run_executor_once` descarta o código de saída (`say "executor rc!=0"`, sem o número). **A lição é sobre o buraco, não sobre a causa: morte por sinal é o único desfecho que este harness não sabe contar.**
  - **Dois achados adjacentes, não corrigidos aqui.** (1) O prompt lista os gates pelo NOME (`typecheck_root`), a allowlist de ferramentas concede `Bash(npm run typecheck:root:*)`, e o agente do 227 tentou `npm run typecheck` — **negado três vezes**, 22 turnos gastos contra uma parede de permissão antes de abortar. Nomear no prompt o comando exato de cada gate custa uma linha em `build_prompt`. (2) `claude_max_turns: 40` está no config e **ninguém passa `--max-turns` na chamada do executor** (só o juiz, com 1): é config morto. Nenhum dos dois foi tocado porque a peça 0b é sobre requeue; entram na próxima que mexer no prompt.
---
- **2026-09-03 (Etapa 3, sessão A)** — Instrumentação e consertos de harness, em worktree dedicada (`harness/etapa3-a`), fora do loop: diff revisado à mão antes do merge (regra 20).
  - **Peça 1 · `liberacao_ok` passa a ler `.tokens[]` com o token INTEIRO.** O bug estava no HARNESS, não no arquivo: `liberacoes.json`, a doutrina e o lint do mapa sempre usaram `{"tokens":["humano:<token>"]}`, e a função consultava `.liberadas[].token` sem o prefixo — nenhuma dependência `humano:*` resolveria, em silêncio. `deps_resolvidas` passa o token inteiro (`humano:x`), não mais `${dep#humano:}`. Formato antigo aceito por UMA versão com dois `AVISO` no log, para não invalidar liberação de outro repo em migração. O WARN do check 10 de `lint-mapa.py` mudou de alvo: não cobra mais o formato do ARQUIVO (que já estava certo), cobra que `lib.sh:liberacao_ok` leia `.tokens` — a asserção que teria pego o bug no dia em que ele nasceu.
  - **Peça 2 · `status_set` e `event` nascem ao lado do `log`.** Instrumentação, não reescrita: nenhuma decisão de gate mudou. `STATUS.md` é derivado de `runs/.status.json` e reescrito INTEIRO a cada transição (tmp + `mv`), então não existe caminho de append; `FILA` e `STAGING` não são campos guardados, são relidos do disco/git a cada render (regra 9) — placar velho num snapshot é pior que placar nenhum. `ocioso` zera TICKET e FASE e exige `MOTIVO`, que é o campo que evita diagnóstico sobre estado velho. `motivo=` dos eventos é a `causa` que o `decisao.ts` já devolve (`enforcement`, `diff_cap`, `criterio_qualidade`, `rate_limit`…): token estável e grepável, sem inventar vocabulário novo. `fora_do_pathspec=` sai do `enforcement.json` — é o remédio 2 da colisão de teste que faltava desde o 201.
  - **Peça 3 · `scripts/orq`, read-only por construção.** Bash, sem dependência nova, reusando `lib.sh` (fonte única de raízes e config). Escrevem exatamente dois subcomandos, num arquivo só: `pausar`/`retomar` no `pausar_file`. Nenhum subcomando toca ticket, staging, lock ou worktree, e nenhum mata processo — a doutrina é explícita: `kill -9` garante ticket órfão. `orq erro` escolhe o recorte pela ORDEM DO PIPELINE (enforcement → gates → critérios → agente), 30 linhas, porque quem reprovou primeiro é quem explica; colar log inteiro produz diagnóstico errado no interlocutor. `orq mapa lint` delega ao `lint-mapa.py` e propaga o rc — não reimplementa regra de lint.
  - **Peça 4 · kill switch pelo `pausar_file` do config, e o executor deixa de apontar para a SKILL.** `pausa_ativa` passa a aceitar os DOIS caminhos — `pausar_file` do config (`docs/fila/PAUSAR`, fonte única, é o que a doutrina e o `orq pausar` usam) e o `.orq-pause` v1 —, senão uma pausa já colocada por alguém seria silenciosamente ignorada por esta mudança. A leitura do início da drenagem vem **antes do reconcile**: o reconcile escreve status de ticket, e pausar e mesmo assim ver a fila mudar é o que faz alguém deixar de confiar no kill switch. `executor.sh:233` passa a apontar `docs/orquestrador/skill/EXECUTOR.md` com "NÃO leia SKILL.md nem references/" — 23k de contexto que não muda o veredicto e onde o executor tem onde se perder (regra 18); nada mais no prompt mudou, e o teste cobra isso item a item.
  - **Peça 5 · orçamento vira gate; toda chamada paga é medida.** As duas chamadas a `claude -p` do harness (agente do ticket e sondagem do preflight) passam a `--output-format json` — o diff continua saindo do `git`, nunca do stdout — e cada uma grava `{data, papel, ticket, attempt, tokens_in, tokens_out, tokens_cache, custo_usd}` em `runs/custo.json`. Os NOMES dos campos vêm de `orcamento.campos_usage`: mudou o CLI, muda o config, não o shell (teste cobra que `total_cost_usd` não apareça em `lib.sh`). O gate roda ANTES da worktree e ANTES da sondagem: teto do dia adia o ticket com `adiado_ate` = próximo dia e encerra a drenagem em `ocioso` `MOTIVO orcamento`; teto por ticket adia só ele e a fila segue. Adiamento por orçamento **não passa pelo contador de retry** (regra 3), e `deps_resolvidas` passa a respeitar `adiado_ate` — assim os dois seletores de ticket (executor e drenagem) herdam a regra de um lugar só. Contramão deliberada: **falha de parse do usage nunca bloqueia**; loga e segue, porque trocar entrega por medição é piorar o problema.
  - **Peça 6 · `canal_notificacao` resolvido: `notificacao_nativa` (decisão humana, registrada no config em `_canal_notificacao`).** `osascript display notification` no `DRENAGEM_FIM`, título com o placar e corpo com duração + `ÚLTIMO`. O **fallback em `runs/notificacoes.log` é escrito SEMPRE**, inclusive quando o `osascript` funciona — a armadilha conhecida é a inversa: aviso que só existe no canal, e no dia em que o canal falha (máquina sem GUI, sessão do launchd sem Notification Center) ninguém descobre que a drenagem acabou nem consegue reconstruir depois. Sem webhook e sem e-mail: credencial a mais para vazar e canal a mais para falhar em silêncio. Provar "osascript ausente" numa máquina macOS exigiu um PATH montado com link para tudo do sistema MENOS o `osascript` — foi isso ou um seam de teste dentro do `lib.sh`, e seam de teste em código de produção é caminho que só existe no teste.
  - **Peça 7 · agendamento por launchd, adaptado — não copiado — do comarka-operacional.** Duas divergências vieram da evidência deste repo, não de estilo: (1) o wrapper **não carrega env nenhum** (o de lá faz `source $HOME/.env.orquestrador`), porque aqui `env_key_present`/`env_value` já leem `<repo>/.env.local`, onde as `preflight_env_keys` moram — dois caminhos de env é o loop rodar contra ambiente diferente do que a trava de identidade conferiu; (2) a pausa citada é o `pausar_file` do config, não o `.orq-pause`. Mantidos por serem incidente pago lá: `caffeinate -i` (sono no meio do run), `node` pelo `NODE_DIR`/PATH (launchd não herda PATH de shell), `claude` conferido ANTES de chamar o loop, e **zero lock próprio** — dois locks é deadlock. O plist fica fora do repo (`docs/orquestrador/launchd.md` traz o exemplo; teste garante que nenhum `.plist` está versionado). **"Outro loop rodando" é o lock `docs/fila/runs/.local-loop.lock` DESTE checkout, nunca `pgrep`/`ps aux` global** — o grep global pega o orquestrador do outro repo, que é operação normal nesta máquina; teste cobra a ausência de `pgrep|pkill|ps aux` no harness inteiro.
  - **Peça 8 · config e docs alinhados ao que o disco mostra hoje.** `sentinela.smoke_cmd` sai de vazio para `npm run smoke`: o script existe e o `/api/health` virou público no 206 — o `_descricao` que dizia "não há endpoint de health neste repo" ficou falso naquele merge. A sentinela **segue desligada** e `fontes` continua só com `suite_completa`: ligar fonte antes do loop que a consome é configuração que ninguém exercita. `SMOKE_URL` vem do ambiente, nunca do config. No ADR-0003, a frase "B2-F1 usa 8 paths, no teto de `max_paths_allowlist`" cobrava do nível errado — o teto de 8 é **por ticket**; a frente é a união dos tickets dela e fechou com **10**, com o maior ticket (201) em 7. Era a mesma confusão que já tinha virado regra inexistente no lint do mapa em 2026-09-03.
  - **Pré-voo 2026-09-03 (seções 0 e 8), em `docs/fila/runs/` — ignorado pelo git por desenho.** Viraram GO: 8.5 (`orq custo`), 8.7 (EXECUTOR.md agora INJETADO, não só presente), 8.14 (notificação) e — não previsto — **8.11**, que virou GO junto com o `orq mapa lint` da peça 3. 8.6 passou a ser provado ponta a ponta: a saída real de `claude -p --output-format json` desta máquina atravessou `custo_registrar` e virou registro correto em `custo.json`. Seguem NO-GO, os dois de autoalimentação e os dois da sessão B: **8.8** (repo map ausente) e **8.9** (`orq gate` não existe; o teste vermelho dos tickets segue verificado à mão). **Buraco novo anotado: `paths_harness` não cobre `scripts/orq` nem `scripts/roadmap/**`** — `scripts/orquestrador/**` não os alcança, e os dois são harness pela regra 20.
  - **Peça 9 · a fronteira adiar/reprovar passa a ser testada contra o envelope REAL, não contra string inventada.** Os dois fixtures de `test/fixtures/claude-envelope/` foram **capturados** desta máquina (CLI 2.1.259, rc=1 nos dois): 429 com `ANTHROPIC_BASE_URL` num servidor local devolvendo `rate_limit_error`, e conexão recusada com a base numa porta fechada. A captura derrubou a premissa: **nem `rate_limit_error` nem `ECONNREFUSED` aparecem no envelope** — o CLI traduz o erro da API para prosa (`"API Error: Request rejected (429) · This request would exceed your organization rate limit"`, `"API Error: Connection refused — a firewall or proxy may be blocking it (ConnectionRefused)"`). O que segura o adiamento hoje é essa PROSA EM INGLÊS mais o `(429)`; o campo estruturado e confiável (`api_error_status`) existe no envelope e **ninguém o lê**. Reescrita de mensagem no CLI derruba o adiamento em silêncio — anotado, não corrigido nesta peça. Segunda armadilha do envelope: erro sai com `is_error:true` e **`subtype:"success"`** — `subtype` não é sinal de nada.
  - **Peça 9 · saída VAZIA com rc≠0 passa a ADIAR (`sem_envelope` no `lib.sh`, gêmeo em `decisao.ts`).** Com `--output-format json` um CLI que chegou a rodar SEMPRE imprime envelope, inclusive em erro — os dois fixtures provam. Zero byte com rc≠0 é o CLI morrendo antes de falar: login caído, credencial expirada, binário ausente, processo morto. Antes isso caía em `exit N` → `criterio_qualidade` → **reprovado**, queimando retry de um ticket que nunca foi julgado; mesmo desenho de falha do rc=130. Classificado como `sessao` para o config seguir sendo a autoridade (tirar "sessão expirada" de `causas_que_adiam` desliga junto). Contrapartida aceita: com login caído o preflight **adia com cooldown** em vez de abortar — adiar se recupera sozinho, reprovação não. Erro de USO do CLI (`unknown option`) **não** entra aqui: o `claude_run` faz `2>&1`, o stderr cai no mesmo arquivo e o arquivo não fica vazio. Provado por A/B: desligada a regra, caem 5 testes e só eles.
  - **Peça 9 · o fail-open do orçamento fica explícito e barulhento.** O veredito já devolvia `ok` com ledger ausente ou corrompido (`custo.test.ts` cobria os dois casos), mas **em silêncio** — e `ok` silencioso de arquivo ilegível é indistinguível de `ok` de consumo zero, que é como um teto deixa de valer por semanas sem ninguém notar. Agora ausente, vazio, corrompido e JSON de forma errada (`{"dias": 42}`) devolvem `ok` **com aviso no `log`**, e o veredito continua saindo sozinho no stdout (o aviso vai para stderr — o `orcamento_veredito` é lido por `$(...)`). O teste passou a cobrar também o nível da drenagem: com ledger corrompido o executor **é chamado**. Fail-open não é fail-always: ledger legível acima do teto continua parando a fila.
  - **Peça 9 · LACUNA: o juiz não existe.** A pergunta era se o avaliador chama `claude -p` em `text` ou em `json` — a resposta é nenhum dos dois. Há exatamente **duas** chamadas ao modelo no harness (agente do ticket e sondagem do preflight), as duas em `json` e as duas seguidas de `custo_registrar`; o teste `as duas chamadas (agente e sondagem) usam json` fixa esse número em 2. `avaliador_model: "opus"` está no config e entra no probe de papéis, mas **nenhum código o consome**: critério com `espera: "avaliador"` é PULADO em `executor.sh:308` (`[ "$esp" = "avaliador" ] && continue`) e não vira `criteriosFalhos`. Ou seja, hoje não há custo de juiz fora do `custo.json` porque não há juiz — todo julgamento é mecânico. **Consequência a não esquecer: critério `avaliador` num ticket hoje é critério que não roda nem reprova.** Quando o juiz nascer: `--output-format json` + `custo_registrar juiz <id> <attempt>`, e o teste dos dois `json` vira três — senão o custo do juiz escapa do gate de orçamento, que é o único freio de gasto do loop.
  - **Peça 10 · `test-preflight.sh` deixa de gastar dinheiro toda vez que roda.** A seção 1 chamava `probe_modelos` com o config REAL afirmando que ele abortava por papel `PENDENTE_*`. Quando os três papéis foram preenchidos o cenário virou falso — e a consequência não foi só um teste vermelho: **sem o abort, o probe seguia até o `claude_run` e fazia uma chamada PAGA**, gravada no `custo.json` do checkout principal (4 registros de `probe`, US$ 0,34, no dia em que isto foi descoberto — dois deles desta própria sessão, rodando o script para diagnosticar). Script de teste que gasta a cada execução é pior que teste quebrado. A proteção não é obsoleta — vale para qualquer papel que volte a ficar pendente —, então o cenário mudou de casa: roda num **ROOT sintético** (`mktemp -d`, fora de git, config real com um papel trocado por `PENDENTE_FIXTURE`), onde o abort acontece antes de qualquer gasto, e ganhou checagem explícita de que **nada foi gravado em `custo.json`**. O config real fica com o que lhe cabe: a asserção de INVARIANTE de que nenhum papel é marcador hoje. A seção 3 também estava cega — conferia `runs/000` no checkout PRINCIPAL, onde attempts de tickets reais vivem por desenho; passou a conferir o `runs/` do fixture. **Lição transferível: teste cujo cenário depende de um estado transitório do config real caduca em silêncio, e o modo de falhar pode ser gasto, não vermelho.**
  - **Auditoria retroativa dos 7 tickets v2 mergeados (200–206), só leitura.** Feita porque **o juiz nunca rodou**: até aqui todo "aprovado" saiu de gate mecânico, e ninguém leu o diff dos testes procurando afrouxamento. Resultado: nenhum `.skip/.only/.todo`, nenhum teste apagado, nenhum arquivo de teste removido, e um único `expect` deletado em toda a série — o do 201, que troca `compliance.aplicado` de `false` para `true` **com comentário explicando o porquê** e mantendo `aplicarCompliance: false` na entrada como prova de que o campo não decide mais nada; endurece, não afrouxa. **Um achado: o 204.** O ticket existia para "provar na web que o gate 205 não tem interruptor", e o teste que ele entregou afirma a AUSÊNCIA de três strings que já não existiam na árvore em que ele rodou — o checkbox tinha sido removido pelo 201, e `'Gerado sem checagem de compliance OAB'` **nunca existiu em forma nenhuma**. Enquanto isso o resto REAL do interruptor — `'· gerado sem compliance OAB'` e o selo `'sem compliance OAB'`, em `RoteirizarForm.tsx:310` e `:383` — não foi afirmado por ninguém e sobreviveu até o 205 removê-lo. O teste também não tem controle positivo: renderiza com `formatos/personas/angulos` vazios e só faz `queryBy…toBeNull()`, então passa igual se o componente não renderizar nada. **Regra: critério de AUSÊNCIA exige (a) prova de que o alvo existia na base do ticket e (b) uma asserção positiva no mesmo render.** É exatamente a classe de coisa que gate mecânico não pega e juiz pega — e o primeiro caso concreto a favor da peça 1 da sessão B.

- **2026-09-03 (Etapa 3, sessão B)** — O juiz e as peças que mudam o que o loop DECIDE, em worktree dedicada (`harness/etapa3-b`), fora do loop, com o lote 206-210 drenando no checkout principal. Diff revisado à mão, peça por peça (regra 20).
  - **Peça 1 · o juiz existe.** Entrada: diff cru + `gates.txt` + TODOS os critérios, inclusive os `avaliador` — que até aqui eram PULADOS em `executor.sh` e não viravam nada. Nunca o prompt do executor, nunca o stdout do agente; o teste cobra os dois negativos lendo o corpo de `run_juiz`. Chamada sem ferramenta (`--allowedTools ""` **mais** instrução explícita no prompt) e `--max-turns 1`: sem a instrução o modelo gasta turno tentando ler arquivo que não vai conseguir. Nível por classe de risco (path do DIFF, palavra do objetivo, tamanho, retry final) — **a dúvida sobe, nunca desce**. `custo_registrar juiz` obrigatório, e **o teste que fixava 2 chamadas ao modelo passou a fixar 3**: é essa contagem que impede o custo de um papel novo de escapar do gate de orçamento, o único freio de gasto do loop. Papel novo que chame modelo entra ali junto.
  - **Peça 1 · veredito ilegível ADIA, e a chamada limitada entra pelo mesmo caminho.** Campo próprio `juizIlegivel`, lido no `decisao.ts` **antes** de qualquer causa de mérito — o juiz roda depois de o agente sair com rc=0, então o curto-circuito de `exitCode === 0` engoliria o sinal. O rótulo entrou em `causas_que_adiam` para o config seguir sendo a autoridade, como o `sem_envelope` da sessão A. Reprovar por veredito ilegível queimaria retry de um ticket que nunca foi julgado.
  - **Peça 1 · módulo com CLI de efeito colateral não é importável — bug real achado no caminho.** `enforcement-core.ts` disparava sua CLI só por `--run-cli` estar no `argv`, sem conferir QUEM foi invocado. Importar `globToRegExp` dele em `juiz.ts` fazia `npx tsx juiz.ts --run-cli nivel` cair na barreira: ela lia stdin, não achava `config.zona_proibida` e derrubava o processo com `TypeError`. O guard passa a exigir também que `process.argv[1]` termine em `enforcement-core.ts|js`; `juiz.ts` e `diagnostico.ts` nascem com o mesmo guard. **Regra: todo módulo deste harness com bloco `--run-cli` confere o ARQUIVO invocado, não só a flag** — senão a primeira vez que alguém o importar, o import vira execução.
  - **Peça 2 · reprovação mecânica vira `refatiar` e para de consumir retry (regra 19).** Diff acima do cap, arquivo fora da allowlist e zona proibida deixam de ser `reprovado`. Repetir a tentativa nunca conserta TAMANHO nem FRONTEIRA — foi assim que o 201 gastou três ciclos para chegar a uma conclusão que era do humano. `notas_status` lista os arquivos, evento `REFATIAR motivo= arquivos=`, commit `fila: <id> refatiar` e, para `origem: humano` (toda a fila hoje), uma linha em `decisoes-pendentes.md` com a pergunta acionável e o que a máquina viu. `origem: planejador` **não** gera decisão pendente: volta ao lote da frente. `refatiar` é classe própria no `fila-read` — cair em "pendente" faria o relatório prometer trabalho que não vai acontecer. O contrato de NÃO-ESCALAR modelo por tamanho ou fronteira continua testado, com o veredito mecânico entregue direto ao `decidirRetry`.
  - **Peça 2 · `gates.txt` guardava placar, e placar não tem nome de arquivo.** O gate de testes reprovava com `FALHA testes_por_pacote [8 pacotes, 861p/1f]` e nada mais — sem uma linha dizendo QUAL arquivo quebrou. Por isso o remédio 2 da colisão de teste era impossível de implementar desde o 201: não havia de onde tirar o nome para comparar com a allowlist. O motor passa a anexar o **recorte** do gate que reprovou (recorte, nunca o log inteiro), e as linhas do recorte não casam `^(ok|FALHA) +<nome>`, então `gate_marca` segue lendo igual.
  - **Peça 2 · o retry passa a receber ONDE, não só o quê.** JSON ≤ 500 tokens com gate, arquivo, linha, esperado vs obtido, trecho ≤ 15 linhas, `criterios_falhos` (inclusive os do juiz) e `fora_da_allowlist`. Falha ao montar o JSON **não** custa o retry: cai no texto de antes. Armadilha medida ao escrever o parser: o vitest emite `- Expected` / `+ Received` em linhas SEPARADAS dos valores, e um regex ingênuo `Expected[:\s]+(.+)` atravessa a quebra e captura `+ Received` como se fosse o valor esperado. A linha do `AssertionError` é a única com os dois valores juntos — é ela que vem primeiro.
  - **Peça 2 · a evidência do 201 provou o buraco, e o que faltava dela provou-o melhor.** Os fixtures são cópia byte a byte de `runs/201/`; a saída bruta do gate de testes **não existe lá**, exatamente porque o `gates.txt` da época só guardava o placar. Foi capturada agora, nesta máquina, e `test/fixtures/runs-201/PROCEDENCIA.md` diz arquivo por arquivo o que é real do 201 e o que é captura de hoje. Os testes provam os dois lados no mesmo arquivo: com o `gates.txt` antigo `fora_da_allowlist` sai vazio; com o recorte, o arquivo é nomeado.
  - **Peça 4 · trailer `Orq-Ticket` no merge e no commit do agente.** A quarentena da sentinela e o relatório precisam saber que arquivos um ticket tocou, e a fonte é o git (regra 5) — mas o trailer não existia em commit nenhum. Entra em parágrafo próprio, **ao lado** da marca `(ticket <id>)` que o `reconcile_merged` já procura: são leitores diferentes e nenhum substitui o outro. O NOME vem de `executor.trailer_commit`, nunca escrito à mão no shell, e o teste cobra isso olhando só as linhas executáveis. `orq ticket` ganha a busca, **ancorada**: `901` não pode casar `9012`.
  - **Peça 5 · a mesma tentativa tinha dois números.** STATUS mostrava "tentativa 1/3" e a trilha gravava `attempt=0`. Como "aprovação de primeira" é contada da trilha como `APROVADO com attempt=1`, a métrica dependia de qual das duas convenções quem contasse tivesse lido. Trilha passa a 1-based (`RETRY` anuncia a PRÓXIMA, então sai `+2`); **diretórios `attempt-N` ficam 0-based** porque renomeá-los invalidaria evidência já gravada de ticket antigo (regra 8), e o mapeamento é impresso por `orq ticket` — `tentativa=3 (attempt-2/)` —, que é onde um humano lê os dois juntos. `orq custo` ganha `DE PRIMEIRA`, lido da trilha e **não** do ledger: a métrica não depende da contabilidade, e escondê-la por falta de `custo.json` seria trocar uma medição por nenhuma.
  - **Peça 6 · os dois scripts de teste ganharam o caminho novo e NÃO foram rodados** (drenagem viva no checkout principal). `test-preflight.sh` seção 5 roda o juiz inteiro por `--stub-juiz` num ROOT sintético e termina conferindo que `custo.json` continua vazio — a mesma proteção que a seção 1 ganhou depois de o probe ter cobrado US$ 0,34 rodando o próprio script de teste. `test-drenagem.sh` roda `drenar` inteira num ROOT sintético para o caminho de `refatiar`, porque `drenar` ESCREVE status e commita.
  - **Pré-voo `runs/pre-voo-2026-09-03-B.md` (ignorado pelo git, por desenho).** Viraram GO: juiz, `refatiar`, diagnóstico estruturado, trailer e numeração. Seguem NO-GO **8.8** (repo map) e **8.9** (gate de ticket), os dois agora da sessão C — a peça 3 foi adiada quando o diff sem testes chegou a 853 linhas nas duas primeiras peças, para isolar a primeira drenagem do juiz de qualquer outra mudança no caminho pré-gasto. Enquanto 8.9 for NO-GO, **o teste vermelho dos tickets segue verificado à mão**.

- **2026-09-03 (lote 4)** — Primeiro lote escrito **para o juiz** e primeiro com **gate humano de migration real**. Fila 200-210 done; B2-F2 e B2-F3 `concluida`, B2-F1 reaberta só pelo 211.
  - **Critério `avaliador` deixa de ser prosa e vira instrução verificável.** O juiz vê diff cru, saída dos gates e o ticket — nada mais. Então todo `avaliador` deste lote diz **o que olhar, em qual arquivo, e o que reprova**: "siga o campo `nome` em `personas-read.ts`: ele tem de aparecer na PROJEÇÃO e no objeto devolvido; declarar só no tipo passa em qualquer grep e entrega null em produção". Critério que diz "cobre integração" sem dizer onde é critério que o juiz não consegue aplicar.
  - **`humano:migration-0027` é o primeiro gate humano que de fato trava um ticket.** O 212 produz só o `.sql`; 213-215 declaram `["212", "humano:migration-0027"]` e ficam parados até a aplicação humana via MCP entrar em `liberacoes.json`. É o primeiro exercício real de `liberacao_ok` depois do conserto da sessão A — até aqui os dois tokens liberados eram históricos.
  - **Alvo especificado que já estava verde: pego pelo recon, não pelo gate.** O 211 nasceria com "asserções positivas no arquivo = 0 → ≥3"; o arquivo tem **5**, todas nos dois `describe` de `ResultadoFinalView`. O zero é **escopado** no `describe` do formulário. **Regra: contagem que mede um arquivo inteiro não mede o bloco cego dentro dele** — escopar o alvo é o que separa critério de teatro.
  - **Premissa de ticket derrubada pelo recon: `WizardGerar` não aceita resultado por prop.** Ele recebe `personas`, `formatos`, `iaReal` e guarda o final em `useState` alimentado por Server Action, que o teste mocka inerte. "Renderizar o wizard num estado com resultado" não é alcançável; quem cobre resultado final é `ResultadoFinalView`, já com controle positivo. **Antes de escrever critério sobre render, leia a ASSINATURA do componente.**
  - **Colisão comportamental: `test/scoring.test.ts` quebra por desenho com a diversificação do 217.** `scoring.test.ts:20-23` afirma `rank[i-1].score >= rank[i].score` para toda a lista — é exatamente o contrato que o top-N diversificado muda. O arquivo estava **fora** da allowlist da frente. Entrou na do 217 e na da frente, com `avaliador` anti-afrouxamento exigindo valor **recalculado** com o porquê em comentário citando o ticket. É a lição do 201 aplicada **antes** de custar ciclo.
  - **Colisão por símbolo tem que ser lida, não só contada.** `git grep -ln 'matchKeywords' -- '*.test.*'` trouxe 4 arquivos; dois (`apps/web/test/angulos-features.test.ts`, `home-blocos.test.tsx`) usam o nome como **valor de entrada hardcoded** de um helper de exibição e não chamam o motor — dependem da CHAVE, não do cálculo. Contar arquivos teria inflado a allowlist do 216 de 2 para 4 sem necessidade; ler cada um mostrou que o contrato a preservar é o nome da chave.
  - **O defeito não aparece na fixture existente — e isso é informação, não obstáculo.** Medido: `matchKeywords` vale 0.71/0.44/0.57/0.50 com os sinais do repo, **nenhum zerado**, porque eles repetem as palavras-chave. O teste vermelho do 216 precisa de fixture própria com sinônimos. Já o defeito do top-N aparece: **top-3 com 1 dor distinta** nos dois pipelines, persona com 3 dores. **Meça antes de afirmar que o ticket é vermelho.**
  - **Terceiro defeito de B1-F3 ficou FORA do lote, com uma linha na entrega da frente** ("peso de formato: reabrir após medição com dados"). Ticket sem alvo vermelho verificado não entra — nem quando a frente o promete.
  - **`@tsv` do jq dobra a barra invertida.** Meu primeiro verificador dos critérios leu os `cmd` com `@tsv` e viu `\\(` chegar ao `awk`, dando "regex inválido" em critério que estava correto. É o mesmo motivo pelo qual `run_criterios` usa `join("\u001f")` desde o ticket 005. **Verificador de critério lê com o separador do executor, ou testa o próprio verificador.**

- **2026-09-03 (lote 5)** — Primeiro lote com **duas frentes de blocos diferentes em paralelo** (B2-F5 e B1-F1), e primeiro em que uma decisão de produto pendente virou ADR antes de virar ticket. Writeback: B2-F1, B2-F4 e B1-F3 `concluida` (200-217 done); B2-F5 e B1-F1 `pronta`; fila 218-226.
  - **TK-201 decidido pelo dono e registrado no ADR-0006: os 16 formatos são 16 ESTILOS, não 16 canais, e mapeiam em 4 arquétipos de esqueleto.** O catálogo já dizia isso num comentário (`catalogo.ts:6-9`: todos os 16 com `tipo_tecnico: 'reels'`, "default Reels aplicado a todos") e ninguém tinha decidido o que fazer com a informação. O mapa estilo → arquétipo vive **em código**, com teste que falha se um dos 16 slugs sumir: em tabela, um estilo novo mudaria a geração por INSERT, sem review e sem teste. **Decisão registrada em ADR é gate cumprido** — `humano:tk-201-arquetipos` entrou em `liberacoes.json` na mesma sessão em que o ADR foi escrito.
  - **Paralelismo de frentes é medido em path, não em intuição.** B2-F5 vive em `services/persona-roteirizador/`, B1-F1 em `services/orquestrador/` + `apps/web/{angulos,lib,server}`. Interseção conferida path a path antes de abrir as duas: **zero**. A regra que sobra: duas frentes só correm juntas depois de alguém comparar as allowlists, e a comparação é do LOTE, não do mapa — a allowlist da frente é a união dos tickets e cresce quando o lote é escrito.
  - **Allowlist de frente cresce quando o lote é escrito, e o lint pega o resto.** B2-F5 precisou de `estagio-roteiro.ts`, `geracao-guiada/tipos.ts` e `entrega.ts`; B1-F1, de mais seis paths. Ampliar a allowlist obrigou a virar `cria_novo: true` em B1-F1 — a regra 6 do lint (todo glob de frente sem `cria_novo` casa ≥1 arquivo no disco) reprovou a primeira tentativa. **O lint do mapa é o que impede allowlist de frente de virar lista de desejos.**
  - **Alvo que já estava verde, de novo — e desta vez por COMENTÁRIO.** O 224 nasceu com `grep -c 'RealPerplexitySource' organico.ts >= 1`, que já valia **1** hoje: a única ocorrência está num comentário (`organico.ts:273`) explicando por que o default caía em fixture. Corrigido para `new RealPerplexitySource(` — construção, não menção. **Regra: alvo por nome de símbolo casa comentário; ancorar na construção, na chamada ou na assinatura.** É a mesma classe do achado do 211 no lote 4, com uma variação nova.
  - **Contador honesto tem unidade declarada, e a unidade não é a linha da tabela.** O teto de fonte real do 226 conta em `org_credential_access_log` (0015), que já existe e é escrita pela própria `cred_get_decrypted` — sem migration. Mas uma geração decripta **duas** credenciais (perplexity + serpapi): contar linha dobraria o consumo e barraria a org na metade do teto. A contagem agrupa acessos em janela e conta **gerações**. E o que o log **não** mede — dólar — virou linha em `decisoes-pendentes.md` em vez de virar número inventado: o log registra acesso à credencial, não chamada cobrada, e converter para USD pede tabela nova.
  - **Acima do teto, degradar é melhor que errar.** Decisão do dono no meio da escrita do lote: acima do teto a geração **acontece**, sobre fixture, e o selo de simulação aparece sozinho porque a fonte passou a ser fixture. Erro na cara do operador seria pior que o problema — e o desenho só fecha porque o selo do 225 já deriva da FONTE, não da credencial.
  - **O DoD do bloco B1 estava sendo contrariado por uma linha de código, e a linha estava documentada.** `page.tsx:26` deriva o selo de `perplexity?.estado !== 'configurado'`, com comentário admitindo a aproximação ("ausência de credencial ⇒ o que vem do motor é simulação"). Com credencial cadastrada e fonte em fixture — que é o estado de hoje — o selo **some**, exatamente o oposto da segunda linha do DoD. **Comentário honesto sobre um desvio não é o mesmo que o desvio estar resolvido**; foi preciso ler o DoD ao lado do arquivo para ver.

- **2026-09-04 (roadmap)** — **Faixa de ID por bloco abandonada:** blocos progridem em paralelo e o loop drena em ordem de ID — faixa por bloco imporia prioridade por bloco. **ID é ordem de fila; `bloco` e `frente` são os campos de pertença.** `B1`–`B4` perdem `faixa_ids` em `mapa.json` e a faixa sai dos títulos de `MAPA.md` e `prd.md`; `B0` mantém `000–099` como registro histórico de faixa já fechada.
  - **O lint deixou de exigir faixa e passou a exigir só coerência.** Check 1 vira condicional: `faixa_ids` é opcional e, **se** existir, ainda tem de ser disjunta. Hoje só B0 declara — o check imprime quantos blocos declaram, para a exceção histórica ficar visível em vez de virar silêncio.
  - **Passo 1 do gate de ticket muda de pergunta.** Sai `id` dentro da faixa do bloco; entra **a frente pertence ao bloco declarado no ticket**, além de `bloco` e `frente` existirem em `mapa.json`. Verificado à mão sobre o corpus **200-226** (o gate ainda é NO-GO 8.9, sem implementação): **27/27 passam**. Com a regra antiga, **5 reprovariam** — 216 e 217 (B1-F3) e 224-226 (B1-F1), todos com ID na faixa que era de B2. A regra velha já estava sendo violada na prática desde o lote 4; o que mudou hoje foi parar de fingir o contrário.
  - **Divergência local anotada, como a doutrina manda.** A skill-mãe dá faixa de ID a cada bloco ("o ID sozinho carrega o contexto"); aqui não. A divergência está marcada na `SKILL.md` vendorizada, no passo 1 de `references/autoalimentacao.md` e na saída do planejador (IDs sequenciais globais, continuando do maior ID já usado). Os `templates/` da skill ficaram **intactos**: eles são para outros repos, e o lint novo aceita os dois mundos.

- **2026-09-05 (lote 7)** — **B3 abre**, e com ele a primeira superfície sem sessão do produto além do `/api/health`. Writeback: B3 `rascunho` → `em_andamento` e **B3-F1 `pronta`** (239-244); B2-F5 reabre em `em_andamento` com as duas telas de recorte (237-238); B2-F7 `em_andamento` com os chips (236); B2-F6 ganha o 245 e a divergência `org.ts` × `middleware` sai de `decisoes-pendentes.md`. Fila 236-245, escrita em worktree dedicada (`fila/lote-7`) enquanto o lote 6 drenava no checkout principal.
  - **A regra do token e da expiração, escrita antes do primeiro `.sql`.** Token de **32 bytes** aleatórios (`randomBytes(32)`, 43 caracteres em base64url), **um por roteiro** (garantido por `unique` em `roteiro_id`, não por disciplina de código), **30 dias** de validade e **revogável por `revogado_em`, nunca por `delete`**. Revogar apagando a linha destruiria a distinção entre *link morto* e *token errado* — e é justamente essa distinção que o 242 usa para **não** contar nada a quem tem token inválido. Duas regras andam junto: token inválido, expirado e revogado devolvem **a mesma resposta vazia**, e o token **nunca** aparece em log, evento ou telemetria (só um prefixo de 6 caracteres). Distinguir os três transforma o link num oráculo; logar o token é logar credencial, e quem lê o log entra como se fosse o cliente do escritório.
  - **Precedente de `get_advisors` decidiu a arquitetura da rota pública, não o gosto.** A migration `0011` existe porque o advisor apontou `user_org_ids` e `create_organization` como SECURITY DEFINER expostas via PostgREST a `anon`/`authenticated`; a remediação foi mover a função para o schema `private`. Fazer `aprovacao_por_token` com `grant execute to anon` reabriria o mesmo WARN. Por isso a `0030` **não cria função nenhuma e não tem policy para `anon`**: a leitura sem sessão roda no servidor do Next com chave de serviço, filtrando pelo token — o padrão que o 226 já usa em `organico-uso.ts`. O par de tickets 242/244 é o contraste didático: **chave de serviço onde não há sessão** (242, e o token é o único separador de tenant), **cliente autenticado onde há** (244, e a RLS separa).
  - **`/aprovar` e `/aprovacoes` — o nome é o gate.** `casaPrefixo` (`routes.ts:16-18`) exige igualdade exata ou o prefixo seguido de `/`, então a fila interna não vira pública por vizinhança. Um prefixo mais curto (`/aprova`) teria publicado a fila do escritório inteira. O 243 prova a classe **nos dois níveis** — `isPublicPath` (a tabela) e um request sem sessão atravessando `updateSession` (o comportamento) —, porque o primeiro sozinho prova a tabela, não o produto.
  - **O ticket que teria entregado código inalcançável.** O desenho aprovado dizia derivar o recorte de `formato.tipo_tecnico === 'carrossel'`. O recon derrubou: `grep -oE "tipo_tecnico: '[a-z_]+'" catalogo.ts | sort | uniq -c` → **`16 reels`**, e o próprio arquivo admite por escrito (linhas 7-9) que nenhum dos 16 formatos criativos mapeia para carrossel/story/post. Quem carrega o formato de verdade é **`angulos.formato`** (`FORMATOS_ANGULO`), escolhido em `/angulos`. Escrito como estava, o 237 teria passado todos os critérios entregando um ramo que nunca executa. **Alcançabilidade não é uma seção do ticket: é uma medição.**
  - **Reabrir uma frente reabre as que dependem dela.** B2-F5 voltou a `em_andamento` por ordem do dono; o check 8 do lint então derrubou B2-F6, que era `pronta` e depende dela. B2-F6 foi para `em_andamento` — o que, aliás, é o que o disco já dizia (227 e 233 mergeados em `main`). Fica a regra: **mudança de status de frente propaga para baixo pelas deps**, e o lint é quem cobra.
  - **Migration nunca depende do próprio token — vira regra do gate de ticket na sessão C.** O 239 nasceu com `dependencias: ["humano:migration-0030"]`, e isso é **deadlock**: o token só pode ser satisfeito depois que o `.sql` existir e um humano o aplicar, então exigi-lo no ticket que **escreve** o arquivo é pedir que a migration esteja aplicada para poder ser escrita. O repo já fazia certo por hábito e ninguém tinha escrito a regra: **009** (`0026`), **212** (`0027`) e **229** (`0028`) têm todos `dependencias: []`. Quem declara o token é quem **consome** as tabelas novas — 213-215 sobre a `0027`, e agora 240-242 sobre a `0030`. **A regra, na forma que o passo do gate deve cobrar:** *ticket cuja `pathspec_allowlist` é só `supabase/migrations/**` NÃO pode declarar o `humano:migration-<n>` da migration que ele mesmo cria.* Depender da migration **anterior** continua legítimo e é caso real: o **234** declara `humano:migration-0028` porque resolve `nicho_id` por join com a tabela que a `0028` cria — a distinção que o gate precisa fazer é entre *o próprio número* e *um número anterior*, não entre ter e não ter gate. Auditoria do corpus inteiro nesta sessão: **5 tickets** com allowlist só de migration, **1 violação**, corrigida (239).

  - **`actions.ts` só cabe numa frente, e a escolha define o que o lote entrega.** `apps/web/src/app/gerar/actions.ts` aparece em B2-F1 (`concluida`) e B3-F4 (`rascunho`) — sobreposições mortas. Com a derivação do recorte dentro do motor, os tickets 237/238 **não** precisam dele, e ele ficou com B3-F1, viabilizando o 240 (persistir no Finalizar) e mantendo B3-F1 `pronta`. A alternativa — `actions.ts` em B2-F5 — teria custado o ticket 240 e adiado a fila de aprovação inteira para o lote 8.

- **2026-09-05 (lote 8)** — **B1-F2 escrita, e B1-F1 reaberta no mesmo dia.** Sessão de planejamento em worktree dedicada (`fila/lote-8`, depois `fila/lote-8b`), enquanto o lote 7 drenava. Writeback: B1-F2 `rascunho` → `pronta` com allowlist ampliada e entrega reescrita (246-249); depois B1-F1 `concluida` → `em_andamento` com o 250, o que **rebaixou B1-F2 de volta a `rascunho`** — a dep `B1-F2 → B1-F1` cobrando o preço dela no lint (regra 8).
  - **Frente cujo texto de entrega já foi entregue ainda pode estar longe do DoD.** B1-F2 dizia "SerpApi resolvido por org via Vault no caminho web", e isso estava pronto desde 224-226 e a migration `0023`: provider no CHECK de `org_credentials`, em `resolver-credencial.ts`, nos dois seams de chave, em `organico.ts` e na tela de credenciais. O que faltava era outra coisa — `temaParaPersona` mandava `pesquisa organica (fixture) — persona <uuid>` como termo de busca, então `web_trends` e `web_perguntas` devolviam zero para sempre, com a chave paga funcionando. **Reconhecer a frente pelo DoD do bloco, nunca pelo texto da entrega:** o texto envelhece, o DoD não.
  - **A allowlist da frente é hipótese do planejador anterior, e pode apontar para o lugar errado.** B1-F2 listava `apps/web/src/server/credenciais*` (onde não sobrou nada) e não listava `apps/web/src/server/angulos*` nem `apps/web/src/app/angulos/**` (onde mora o defeito). Ampliar é edição do dono, e a condição que ele cravou vira regra: **só amplia se nenhuma outra frente `pronta` ou `em_andamento` já reivindicar o path** — se reivindicar, é dep de frente, não ampliação. `lint-mapa.py` cobre o cruzamento com zona proibida (4) e harness (5), **não** o cruzamento entre frentes: essa conferência é manual.
  - **Grep de critério casa comentário, e o comentário mente sobre o código.** O guard que provava a sobrevivência dos avisos `[DORMENTE]` usava `grep -oE 'FIXTURE SINT'` e contava **3**: uma ocorrência no código e duas em comentário. Ancorar em trecho que só existe no código (`Nenhum sinal ingerido`). Mesmo motivo pelo qual os alvos vermelhos de 249 e 250 casam `hash_dedupe:` **com dois-pontos**: a palavra solta só aparece em comentário nos três conectores.
  - **`toEqual` sobre o retorno de uma função é contrato de FORMA, não só de valor.** `noop-dormente.test.ts` faz `expect(contagem).toEqual({ ingeridos: 0, descartados: 0, duplicados: 0 })` — isso proíbe o 246 de acrescentar campo ao retorno dos plugs, porque `toEqual` reprova propriedade extra. O gancho de degradação entrou por `opts` e o teste intocado virou **controle positivo** em vez de vítima. Antes de projetar mudança de assinatura, ler quem afirma o retorno com `toEqual`.
  - **Dedup sem a persona é um NO-OP com outra cara.** `computeHashDedupe` monta `origem|fonte_tipo|fonte_ref` e `findByDedupe` filtra só por origem e hash: duas personas do mesmo escritório com o mesmo termo colidem, e a segunda fica com zero sinal. Estava escondido porque o termo carregava o UUID da persona — **corrigir o termo sem corrigir o dedup troca um defeito por outro**, e por isso 249 vem antes de 247 e o 250 faz as duas metades no mesmo ticket. O remédio que não quebra nada: computar `hash_dedupe` com a persona e **deixar o `fonte_ref` persistido intacto** — é ele que `f1-6-connector` e `r3-7-connector` afirmam com `toEqual`.
  - **"Qualidade da consulta" e "caminho do sinal real" não são categorias separadas quando a consulta é paga.** O tema com `fixture` e UUID foi primeiro classificado como defeito de qualidade e mandado para `decisoes-pendentes.md`, porque a decisão fixture×real acontece em `organico.ts`, antes, e a credencial não passa por ali. O dono reverteu com a própria evidência da sessão: a string vai **quatro vezes por geração** ao Sonar, cobrada da org, e o que volta é persistido como sinal `juridico` com proveniência de fonte **real** — o selo do 225 diz corretamente "real" sobre um ângulo sem assunto. Virou o ticket 250, e a linha saiu de `decisoes-pendentes.md`.
  - **Ticket já mergeado e ainda `pendente` pode precisar de manutenção quando o plano muda.** O 250 reescreve o tema que o 247 mandava não tocar; sem ajuste, o avaliador do 247 reprovaria trabalho legítimo do 250. O 247 ganhou `250` em `dependencias` e teve o item 3 do objetivo e o 2º avaliador reescritos. **Dep de frente ordena a doutrina; dep de ticket é o que o loop lê** — quando as duas coisas precisam valer, declarar as duas.

- **2026-09-04 (lote 6)** — **Primeiro lote nascido de feedback do dono olhando o produto**, não de decomposição de frente: ele abriu `/personas` em 04/09, tentou salvar uma persona e recebeu *"Sem escritório ativo. Faça o onboarding primeiro."* sendo owner de um escritório com persona, e o formulário zerou junto. Writeback: B2-F5 e B1-F1 `concluida` (218-226 done); B2-F6 e B2-F7 nascem `pronta`; fila 227-235; ADR-0007 (catálogo de conhecimento e sugestões).
  - **O bug era de semântica, e o olho humano foi o único detector.** `apps/web/src/lib/auth/org.ts:14` escreve `const { data } = await …` e descarta o `error`, colapsando três estados — *sem sessão resolvível*, *leitura falhou* e *sem escritório* — num único `null` que a ação traduz como "faça o onboarding". Confirmado que o dado está certo por consulta só-SELECT nesta sessão, rodando a query exata do código **como o próprio usuário** (`set local role authenticated` + `request.jwt.claims`, dentro de `begin/rollback`): a vinculação existe, papel `owner`, e a org tem 1 persona. Nenhuma suíte pega isso — o teste que existiria afirmaria que a função devolve `null`, que é o que ela faz.
  - **Caso a favor do smoke de tela na sentinela.** A `sentinela` tem `smoke_cmd` preenchido desde 2026-09-03 (`npm run smoke`) e `fontes` ainda só com `suite_completa`. Este bug atravessou 27 tickets verdes, dois gates de typecheck e uma suíte inteira porque **nenhuma fonte da sentinela abre uma tela e clica**. Um smoke que autentique e poste o formulário de persona teria falhado no primeiro dia. Enquanto `fontes` não incluir smoke, "verde" quer dizer *a suíte passou*, não *o produto funciona* — e a distância entre as duas coisas é o lote 6 inteiro.
  - **Hipótese do briefing derrubada pelo recon, e registrada como tal.** O briefing supunha que o guard tinha vindo do 207/213 e lia `onboarding_step`. `git grep -n 'onboarding_step' -- apps/web` só aponta `home-read.ts` (rótulo de passo) e `onboarding.ts` (escrita), e `git blame` põe a linha do guard em `a2310a6b`, **2026-06-25**, três meses antes do lote 3. O `onboarding_step='org'` da org do dono é o default que a `0025` deixou numa org anterior ao 207: rótulo defasado, não trava. Escrever o ticket sobre a hipótese teria produzido um alvo vermelho que **passa hoje** — nada lê a coluna.
  - **Fronteira de frente respeitada em vez de contornada.** O 3º ticket de B2-F7 (chips na tela) sobreporia `PersonaForm.tsx` e `personas/page.tsx` com B2-F6, e a doutrina manda sobreposição de frente virar dep de frente — o que impediria F7 de nascer `pronta` (lint, regra 8). A saída de contornar com dep de ticket foi **descartada pelo dono**: exceção que funciona hoje e que o planejador não sabe ler depois, e UI escrita contra um formulário que outro ticket do mesmo lote está redesenhando. O ticket sai do lote e nasce no lote 7, sobre o 231 já mergeado. Frente `pronta` com 2 de 3 tickets escritos é estado legítimo; a allowlist dela cresce quando o 3º entrar.
  - **Colisão de tipo é colisão, e custa allowlist.** O `tsconfig` de `apps/web` inclui `**/*.ts` e `**/*.tsx`, então `apps/web/test` é typecheckado: mudar `PersonaRow.faixa_renda` para lista quebra a compilação de `personas-form.test.tsx:26` e `personas-listagem.test.tsx:19`, que declaram fixtures com `faixa_renda: null`. Os dois entraram na allowlist do 230 **só para a fixture mudar**, com anti-afrouxamento por contagem medida (`it=2/expect=12` e `it=4/expect=11`). Colisão comportamental não é só asserção que muda de valor — é também fixture que deixa de compilar.

- **2026-09-07 (harness, etapa 4)** — Sessão de harness em worktree dedicada (`_worktrees/ci-harness-d`, branch `harness/etapa4`), **fora da fila**, com revisão por diff e um commit por peça, enquanto o loop drenava no checkout principal. Backlog: `docs/orquestrador/PECAS.md`.
  - **Peça 0d · script de teste nunca escreve na trilha de produção.** `lib.sh` resolve `MAIN_CHECKOUT` por `git rev-parse --git-common-dir`, então um `source lib.sh` no TOPO de um `test-*.sh` herda o checkout PRINCIPAL **de qualquer worktree** — e `event`/`status_set`/`custo_registrar` disparados ali gravam na trilha real. Foi por essa porta que entraram os três `EXECUTOR_MORREU rc=1 fase=?` de 2026-09-04 (`events.log` reais 153, 155 e 156: 20:45:00, 20:47:00 e 20:47:09), sem drenagem nenhuma por trás. Tranca dupla: os três `test-*.sh` declaram `ORQ_TESTE=1` + `ORQ_EXEC_ROOT="$(mktemp -d)/checkout"` **antes** do `source` (depois não adianta: `RUNS_BASE` já foi resolvido), e `lib.sh` ganhou `escrita_de_teste_permitida`, que sob `ORQ_TESTE=1` **recusa** trilha, STATUS e ledger fora de `$ORQ_EXEC_ROOT` — silencioso no desfecho, ruidoso no log. O que a peça 10 já tinha cobrado em dinheiro (US$ 0,34 do `test-preflight.sh` contra o config real), esta cobra em evidência falsa.
  - **Evidência falsa é pior que evidência ausente.** `EXECUTOR_MORREU` espúrio na trilha é indistinguível de morte de verdade: quem lê depois conta mortes que não houve e vai procurar causa onde não existe defeito. Por isso a correção é uma GUARDA no `lib.sh`, não disciplina de quem escreve teste — disciplina não sobrevive à sexta-feira.
  - **`ANOTACAO`: a trilha é append-only, e a correção honesta de um registro append-only é uma anotação ao lado.** `lib.sh` ganhou `anotacao <texto>` (evento `--- ANOTACAO nota=…`). Os eventos espúrios de 04/09 ficam onde estão; o que a trilha ganha é a linha que diz o que eles são. A anotar **no checkout principal** (a worktree de harness não toca a trilha real), uma vez:
    `bash -c 'source scripts/orquestrador/lib.sh; anotacao "EXECUTOR_MORREU de 2026-09-04 20:45:00, 20:47:00 e 20:47:09 sao ESPURIOS: vieram de test-*.sh rodando contra a trilha real, nao de drenagem. Corrigido pela peca 0d (ORQ_TESTE + escrita_de_teste_permitida)."'`
  - **Peça 0e(a) · evento na trilha nunca contém `\n`.** O "uma linha por evento" era convenção, não garantia: um `\n` dentro de um valor quebrava o evento, e toda linha a partir da segunda ficava sem timestamp — invisível para `grep`, `tail`, contagem e para o `orq erro`, que lê a trilha linha a linha. Em 2026-09-05 13:40: 427 linhas, 243 eventos, 184 linhas órfãs; trinta minutos antes, 413/237/176. Hoje, 07/09: **670 linhas para 288 eventos**. O escape (`uma_linha`) mora DENTRO do `event()`, não em cada chamador — ponto de estrangulamento é o único lugar onde uma regra de formato se mantém sozinha.
  - **`cut -c` é orientado a LINHA, e por isso o truncamento não bastava.** `executor.sh` já cortava o comando em 160 (`cut -c1-160`) desde a peça 0c; num comando multi-linha isso corta CADA linha em 160 e mantém TODAS — foi assim que o heredoc Python do 234 foi parar nas linhas 254-259 da trilha (evento de 05/09 13:31:45). Escapar ANTES de truncar é o que faz "160 caracteres de comando" querer dizer o que sempre pareceu dizer.
  - **A entrada do teste é o byte real, não uma imitação.** `test/fixtures/trilha/permissao-negada-234.cmd` é o comando da linha 253 da trilha de produção, copiado byte a byte, e `events-quebrado.log` é o recorte 253-382 (130 linhas, 23 eventos, 107 órfãs). Teste de formato escrito com exemplo inventado passa contra o exemplo, não contra o mundo.
  - **Peça 0e(a2) · reparo único de formato do `events.log`.** `reparar-trilha.sh` recolhe cada linha sem timestamp para o evento anterior, com `\n` escapado. **Não é reescrita de história: é correção de formato** — mesma ordem, mesmos bytes, muda só onde estão as quebras de linha. A prova disso é o round-trip do teste: desfazer o escape devolve o arquivo original byte a byte. Guarda cópia crua do antes (`events.log.pre-reparo-<epoch>`) e confere as duas invariantes — 100% timestampado e mesma contagem de eventos — ANTES de trocar o arquivo; se qualquer uma falhar, o original fica intacto.
  - **Reparo que não sabe recusar não é reparo.** Se a trilha COMEÇA com linha órfã não há evento anterior para recolhê-la, e inventar um timestamp seria escrever no lugar da trilha: rc=2, arquivo intocado, caso de humano. E "uma vez só" é por construção, não por disciplina: sem linha órfã o script não escreve, não anota e sai 0 — rodar de novo é no-op, não um segundo `ANOTACAO`.
  - **A contagem de eventos "de antes" não é constante.** O arquivo cresce enquanto o loop drena, então o teste não pode fixar um número: ele tira a contagem imediatamente antes de reparar e compara com a de depois, descontando o `ANOTACAO` do próprio reparo — que entra DEPOIS da contagem justamente para isso.
  - **Peça 0e(b) · a derivação de `allowedTools` entende aspas.** `prefixoDeCmd` cortava no primeiro `|` sem saber se ele estava dentro de aspas — e num `grep -E` ele quase sempre está. Nos 105 critérios reais dos cinco tickets do lote 8 (246-250), **21 prefixos saíam com aspas abertas**: `test $(grep -cE 'export const WARN_(TRENDS|PERGUNTAS)_DEGRADADO' arq.ts) -eq 2` virava `Bash(test $(grep -cE 'export const WARN_(TRENDS)`, permissão que não casa nada e não autoriza nada. `$(…)` conta junto com as aspas: um `|` ali dentro é do subcomando, e cortar nele deixa parêntese aberto — igualmente inútil.
  - **A regressão vale escrita como código no teste.** O teste guarda a derivação ANTIGA como função e mede com ela quantos dos 105 critérios quebravam. O que se afirma deixa de ser "o novo funciona" e passa a ser "o antigo produzia permissão que não autoriza nada, em 21 casos, e agora são zero" — que é o que justifica ter mexido.
  - **Peça 0e(c) · o prompt ganha o bloco "COMO RODAR COMANDOS", e a lista sai da MESMA string do `--allowedTools`.** Três regras: o cwd já é a raiz da worktree e nunca se prefixa com `cd`; um comando por chamada, sem `&&` nem `|` emendando ferramentas diferentes; e os autorizados são exatamente estes — seguidos da lista. O 230 perdeu 6 turnos e o 234 perdeu 4 em `permission_denials` por `cd &&` e por pipes: é a repetição do incidente do 227 por outra porta — lá a permissão não cobria o comando, aqui o agente monta um comando que permissão nenhuma poderia cobrir.
  - **Lista fixa no prompt recriaria, em texto, o desalinhamento que a peça 0c matou no código.** Por isso `tools_do_ticket` é fonte única: `run_attempt` deriva UMA vez e passa a mesma variável para o `build_prompt` e para o `--allowedTools`, e `build_prompt` deriva sozinho quando o argumento falta — não existe caminho que produza prompt sem lista, nem prompt com lista de outra fonte.
  - **Teste de "mesma string" tem de vir da mesma CHAMADA.** O teste roda `run_attempt` de verdade com `claude_run` interceptado, e compara o `prompt.txt` gerado com o argumento `--allowedTools` capturado do argv daquela execução. Duas derivações que o teste junta provariam só que a função é determinística.
  - **Peça 11 · o disparo do launchd deixa de ficar refém de um STATUS congelado.** STATUS em `executando` desde 06/09 12:38 comeu **22 h de disparos**, todos com exit 0: o snapshot dizia `executando` o tempo inteiro e nada estava rodando. `launchd-run.sh` passou a aplicar a STALENESS primeiro — `DESDE` além de `claude_timeout_secs` significa processo morto, porque o `claude_run` mata o grupo ao fim do timeout e nada legítimo sobrevive a ele — recuperando (evento `RECUPERADO` + STATUS limpo) e SEGUINDO; só depois pergunta se há drenagem em curso. E `orq` mostra `MORTO há Xh` em vez de `executando`, trocando a linha NA LEITURA: `orq` é read-only por construção.
  - **A ordem é a peça inteira.** Perguntar "já há drenagem em curso?" primeiro é confiar num estado que ninguém mais está mantendo. Recuperar com processo vivo é seguro: o lock do loop continua sendo a autoridade sobre concorrência, e o disparo que segue encerra em 0 se houver mesmo alguém drenando. O que se recupera é o SNAPSHOT, não a fila.
  - **A guarda por processo é POR REPO, e por isso é o lockfile.** A pergunta vai ao `.local-loop.lock` sob o `runs_dir` deste checkout, nunca a um `pgrep -f local-loop` global — dois orquestradores na mesma máquina são operação normal, e o grep global já pegou o loop do comarka-operacional uma vez (registro de 2026-09-02). `kill -0` é sonda de vida, não sinal.
  - **Comando externo antes do `export PATH` é bug latente.** O wrapper resolvia o próprio caminho com `$(dirname ...)` na primeira linha — e sob o launchd o PATH chega quase vazio. Falhava em silêncio: `REPO_DIR` saía errado e o run só não quebrava porque nada o usava antes do `cd`. Ao passar a *carregar o lib.sh* dali, a falha virou fatal e apareceu. Agora tudo antes do `export PATH` é builtin (`${var%/*}`, `cd`, `pwd`, `command -v`).
  - **Peça 12 · morte por sinal deixa rastro.** O trap de `EXIT` NÃO roda quando o processo morre por sinal não trapeado: o shell termina pela ação padrão do sinal e nada mais é executado. Em 06/09 15:38 UTC o `local-loop.log` traz `gates: REPROVADO (rc=143)` seguido de `Terminated: 15` e **nenhum evento na trilha** — 143 é 128+15. Agora `armar_traps` cobre `EXIT`, `TERM` e `INT`, e `trap_sinal` desarma o `EXIT` antes de sair: uma morte, um registro.
  - **Limpar o STATUS faz parte do registro.** Snapshot que fica em `executando` depois que o processo morreu não é informação velha, é informação ERRADA — e é exatamente a matéria-prima da peça 11, que precisou nascer para recuperar dela. Quem morre desliga a própria luz.
  - **O bash SEGURA o sinal até o comando em primeiro plano voltar.** É por isso que o log de 06/09 mostra a linha dos gates ANTES da morte: o TERM foi para o grupo, o `npx tsx gates.ts` voltou 143, a linha foi impressa, e só então o executor teria tratado o sinal — se houvesse trap. O `claude_run` usa `wait`, que é interrompível; o teste modela os dois caminhos.
  - **Regra de fechamento de sessão de harness.** Toda sessão que mexe no harness fecha com `tail -3 docs/fila/runs/events.log` e `git diff` do `custo.json` **da worktree onde ela rodou**, comparados com o início. Os dois inalterados é a prova de que a sessão não drenou, não gastou e não escreveu na trilha; qualquer linha nova ali é evidência falsa entrando no histórico — e o teste da peça 0d existe justamente para que essa checagem seja redundante, não para substituí-la.

- **2026-09-07 (lote 9)** — **Primeira sessão de fila que não escreve ticket novo: divide dois que o harness não conseguiu fechar.** Worktree dedicada (`fila/lote-9`, a partir de `main`), com o loop parado. `230` (bloqueado, 5 tentativas, 3 modelos) vira `230a`+`230b`; `241` (`refatiar`) vira `241a`+`241b`; os dois originais saem da fila com nota apontando os sucessores, e `231`/`232` passam a depender de `230b`, `242`/`243`/`244` de `241b`.
  - **Ticket bloqueado três vezes não era difícil: era impossível dentro da própria allowlist.** As cinco tentativas do 230 reprovaram no MESMO gate com os MESMOS três erros — `PersonaForm.tsx:101` e `personas-form-preserva.test.tsx:31,45` deixam de compilar quando `PersonaRow.faixa_renda` vira lista, e nenhum dos três está na allowlist. O agente da última tentativa escreveu o diagnóstico dentro do próprio diff (*"o conserto é de quatro linhas, todas fora daqui"*) e ainda assim reprovou, porque não podia tocá-las. **Antes de conceder retry a um ticket reprovado por gate, comparar o arquivo do erro com a allowlist: erro fora dela não é retry, é ticket mal escrito.** Custo medido: 5 execuções, ~3.000s de agente, zero linha aproveitada.
  - **A colisão foi medida contra a árvore errada, e a dependência declarada não salvou.** O 230 declarava `228` como dep e listou a colisão de tipo do lote 6 — `personas-form.test.tsx` e `personas-listagem.test.tsx`. Mas o 228 **criou** um terceiro arquivo (`personas-form-preserva.test.tsx`) e passou a alimentar `PersonaForm.tsx` com `state.valores`, e nenhum dos dois entrou na conta. **Colisão se mede na base em que o ticket vai rodar — `staging-auto` com as deps já mergeadas —, nunca na árvore do dia em que o lote foi escrito.** Nesta sessão a medição foi `git grep -n 'PersonaRow' staging-auto -- apps/web`, e ela separa quem **constrói** o tipo (quebra) de quem só o **consome** (não quebra): 2 fixtures anotadas contra 2 consumidores.
  - **A saída não foi allowlist maior: foi parar de mudar o tipo.** `PersonaRow.faixa_renda` continua `string | null` (o texto junto, que é o que a tela mostra e o motor recebe) e a lista entra como campo NOVO, `faixas_renda`. Acrescentar campo obrigatório quebra só quem constrói o tipo anotado; trocar o tipo de um campo quebra também todo mundo que o lê. **Numa allowlist apertada, campo novo custa duas fixtures; troca de tipo custa a tela inteira.** Os três campos novos são obrigatórios de propósito — opcional deixaria a leitura esquecer a coluna sem ninguém perceber, que é o defeito que a projeção explícita existe para impedir.
  - **`refatiar` não quer dizer allowlist nem tamanho.** O 241 voltou com os **cinco gates verdes** (1436 testes, build) e 438 linhas, dentro do teto de 600. Quem devolveu foi a barreira de credencial (`enforcement-core.ts:195`): três fixtures escritas como `token: 'token-vivo-ja-existente-000000000000000000'`, que é literal de 16+ caracteres com letra e dígito atribuído a uma chave chamada `token`. A regra está certa — token de mentira e token de verdade são indistinguíveis para quem varre o diff —, e o ticket é que precisa dizer como se escreve fixture de token: **por expressão (`'vivo'.padEnd(43, 'x')`), nunca literal**, porque o token precisa dos 43 caracteres para o teste ser honesto sobre o tamanho mínimo. Os dois tickets novos carregam a regra no objetivo e um guard que a mede antes do harness. **Ler `enforcement.json` antes de concluir por que um ticket voltou: `notas_status` diz "resolver ampliando a allowlist ou fatiando", e nas duas vezes a causa era outra.**
  - **O ponto de costura entre as duas metades é um caso de teste, e ele tem de estar declarado.** O 241a deixa `gerarLinkAprovacao` RECUSANDO quando a linha está morta, com teste afirmando a recusa; o 241b troca esse ramo pela reemissão e **precisa editar aquele caso**. Em vez de deixar um ramo sem asserção no 241a ou uma edição fora da allowlist no 241b, o arquivo de teste do 241a entra na allowlist do 241b com anti-afrouxamento por contagem. **Dividir ticket cria colisão entre as metades: ela se declara como dep + allowlist sobreposta, igual a qualquer outra.**
  - **Dividir estoura o teto de tickets por frente, e o lint não vê.** B2-F6 vai a 9 tickets e B3-F1 a 7, contra `tickets_estimados` 8 e 6. O check 3 do `lint-mapa.py` só compara o CAMPO (`tickets_estimados > 8`), nunca conta os arquivos de `docs/fila/` — os dois campos ficaram como estão, porque são estimativa de planejamento, e o registro fica aqui. Se um dia a contagem real virar regra, é o check 3 que muda.
  - **Achado de passagem, anotado no `risco` em vez de corrigido de véspera: o 231 tem a mesma bomba do 230.** Tirar o campo de CTA e trocar faixa/tom por seletor quebra três asserções de `personas-form-preserva.test.tsx` (74, 77, 78), que está fora da allowlist dele. Não foi corrigido nesta sessão porque a correção é replanejar o 231, não religar dep — mas está no campo `risco` do ticket, que viaja com ele.
  - **`obsoleto` não é status: o relatório classificaria como `pendente`.** `fila-read.ts:104` cai em `pendente`/`aguarda` para qualquer status desconhecido, e o `placar_fila` de `lib.sh:210` só conhece `pendente aguardando_merge refatiar bloqueado done`. Ticket fatiado sai da fila com `status: "done"` + `notas_status` começando em `OBSOLETO <data>: dividido em …`, que é o precedente do `001` (fatiado em `001a`/`001b` em 21/08). **Status é vocabulário fechado lido por código; a semântica nova vai na nota, não no enum.**
- **2026-09-07 (harness, etapa 5)** — Sessão de harness em worktree dedicada (`_worktrees/ci-harness-d`, branch `harness/etapa5`), **fora da fila**, com revisão por diff e um commit por peça. Backlog: `docs/orquestrador/PECAS.md`.
  - **Peça 13 · notificação só quando houve o que notificar.** Em 07/09 11:0x o launchd cuspiu um cartão `0 aprovados · duração 0min · último —` a CADA tick enquanto o 240 ainda rodava: a drenagem não achava processável, encerrava em zero e notificava assim mesmo. **Notificação que chega quando nada aconteceu treina o operador a ignorar a notificação que importa** — o custo não é o ruído, é a que ele passa a não ler. Agora `deve_notificar` (no `lib.sh`) decide: processou ≥ 1 ticket notifica sempre; fila sem processável notifica só na PRIMEIRA vez desde a última drenagem que processou; qualquer outro fim em zero (pausa, cooldown, sem progresso) cala. Medido no teste: com o comportamento antigo, dez ticks com a fila vazia escreviam **dez** linhas; hoje escrevem **uma**.
  - **A memória tem de sobreviver ao processo, e por isso é arquivo.** Cada tick do launchd é um processo novo — variável não atravessa, e é justamente a REPETIÇÃO que se quer calar. O marcador `runs/.notificacao-fila-vazia` é criado quando se avisa "fila vazia" e apagado por qualquer drenagem que processe algo: o rearme é consequência de ter havido trabalho, não de alguém lembrar de limpar.
  - **`EXECUTOR_MORREU` não chega pelo rc.** O executor morre num processo FILHO, e do lado da drenagem `rc != 0` é indistinguível de reprovação mecânica. O registro dessa morte é a trilha (peça 12), então a drenagem conta `EXECUTOR_MORREU` no `events.log` ANTES do laço e DEPOIS dele, e a diferença é o que morreu naquele trecho — morte velha não rearma nada. Contar pelo total do arquivo faria toda drenagem posterior a uma morte parecer produtiva para sempre.
  - **`motivo_ocioso` virou constante compartilhada.** A decisão de notificar compara com a frase `sem ticket processável`, que antes existia solta no `local-loop.sh`. Frase repetida em dois arquivos é dois arquivos que divergem no dia em que alguém a reescreve — `MOTIVO_FILA_VAZIA` mora no `lib.sh` e os dois lados leem a mesma variável.
  - **Refatiar conta como processado, e o PECAS não o listava.** A peça enumera "aprovado, bloqueado, adiado ou `EXECUTOR_MORREU`"; `refatiar` (regra 19) é desfecho de executor que RODOU e gastou dinheiro. Calar sobre ele seria o defeito espelhado — silêncio quando algo aconteceu — então entra na conta, e fica registrado aqui que a lista do PECAS era menor que a realidade.
  - **Peça 1 · gate de ticket (`scripts/orquestrador/gate-ticket.ts` + `orq validar`).** O item **8.9** do pré-voo é NO-GO desde 2026-09-03 (`PLAYBOOK.md:85`), e enquanto for, o teste vermelho de cada ticket é conferido À MÃO — foi assim o lote 8 inteiro. Esta peça paga a metade barata e determinística dos passos 1-6 de `autoalimentacao.md` §2: bloco ```json parseável, campos obrigatórios do pendente, `id` no formato `NNN[a]` e igual ao prefixo do arquivo, `status` no vocabulário, `tipo`/`cmd`/`espera` de cada critério, padrões proibidos no `cmd`, dependência por id existente ou `humano:<token>`, e allowlist sobreposta entre pendentes sem dependência declarada. Saída: uma linha por violação, `<arquivo>:<campo> <mensagem>`, rc 1 se houve; `--relatorio` imprime tudo e sai 0.
  - **Fica de fora POR DECISÃO, e vira peça 1b:** rodar `alvo` contra HEAD para ver se já passa, rodar `guarda` para ver se já falha, e execução de `recon[]`. Tudo que EXECUTA comando escrito por modelo é outra peça e outra classe de risco — e é justamente a parte que o §2 cerca com worktree descartável, timeout e env de teste. Um gate que só LÊ pode rodar em qualquer lugar, inclusive nesta sessão, contra a fila real, sem consequência.
  - **A chave do config é `proibido_no_cmd`, e não `padroes_proibidos`.** O briefing da sessão nomeava `gate_ticket.padroes_proibidos`; `grep` no repo inteiro não acha essa chave em lugar nenhum. O que existe desde a Etapa 1 é `gate_ticket.proibido_no_cmd`, já semeada com exatamente os padrões que o §2 nomeia. Nome de chave é para conferir no disco, nunca lembrar — a regra existe no briefing e foi ela que pegou este caso.
  - **A precedência entre prefixo permitido e padrão proibido virou regra escrita** (decisão do dono, 07/09, registrada em `autoalimentacao.md` §2). O config já anotava o conflito em `_conflito_conhecido` e nunca o resolvia: `proibido_no_cmd` veta `$(`, mas `test $(` está em `cmd_prefixos_permitidos` porque critérios reais usam a forma. Agora: (a) a isenção vale só para o PRIMEIRO segmento — emendou `&&` ou `|`, o veto volta a valer nos seguintes; (b) redirecionamento isento é só `2>&1`, `>/dev/null` e `2>/dev/null`, e `>`/`>>` para arquivo real reprova sempre; (c) `sh -c`, `bash`, `eval`, `sudo`, `rm`, `curl`, `wget` e crase nunca são isentos; (d) a isenção APARECE na saída, com o prefixo que a concedeu.
  - **Isenção silenciosa é como o repo perde a regra de vista.** É por isso que (d) não é enfeite de relatório: as 25 isenções abaixo são 25 lugares onde o gate escolheu não reprovar, e cada uma está nomeada com o prefixo que a autorizou. Regra que se aplica sem deixar rastro vira, dois meses depois, comportamento que ninguém sabe explicar.
  - **`dependencias: []` e `risco: ""` NÃO são campo vazio.** A primeira versão do check 2 reprovava os dois, e os 7 pendentes inteiros caíram por isso. Ticket independente é a coisa certa a ser — 63 dos 74 têm `dependencias: []` —, e `risco` é preenchido pelo **passo 6** do gate, que fica fora desta peça: cobrar valor ali seria o gate reprovando ticket por não ter feito o que o próprio gate ainda não faz. Os dois passaram a exigir só que a CHAVE exista.
  - **A leitura do `cmd` reaproveita a lição da peça 0e-b.** Segmentar por `|`/`&&`/`;` sem olhar aspas quebraria todo `grep -E 'a|b'`, e procurar token por substring faria `grep 'form '` virar o comando `rm`. Então: as aspas são mascaradas por espaço (comprimento preservado, para que índice medido no visível ainda aponte o cru), `$(…)` não é ponto de corte, e token de palavra casa por fronteira, não por `includes`.
  - **Evidência: `bash scripts/orq validar --pendentes --relatorio` contra os 7 pendentes reais, 2026-09-07.** **33 violações e 25 isenções.** Nenhum ticket foi corrigido nesta sessão — corrigir fila é sessão de fila. O padrão é um só e se repete: `test -f <arquivo> && test $(grep -c ...) -ge N && echo ok`, em que o `$(` cai no SEGUNDO segmento e perde a isenção. Saída inteira:

```
GATE DE TICKET · 7 ticket(s) · fila /Users/lucasantos/Projetos/conteudos-infinitos/docs/fila

231-formulario-de-persona-guiado.md  3 violação(ões)
  231-formulario-de-persona-guiado.md:criterios_aceite[1] padrão proibido '$(' em segmento 2 ('test $(grep -c 'it(' apps/web/test/personas-form-guiado.test.tsx) -ge 8') — cmd: test -f apps/web/test/personas-form-guiado.test.tsx && test $(grep -c 'it(' apps/web/test/personas-form-guiado.test.tsx) -ge 8 && echo ok
  231-formulario-de-persona-guiado.md:criterios_aceite[2] padrão proibido '$(' em segmento 2 ('test $(grep -c 'nichos_juridicos' apps/web/src/server/nichos-read.ts) -ge 1') — cmd: test -f apps/web/src/server/nichos-read.ts && test $(grep -c 'nichos_juridicos' apps/web/src/server/nichos-read.ts) -ge 1 && echo ok
  231-formulario-de-persona-guiado.md:criterios_aceite[3] ISENCAO '$(' no segmento 1 pelo prefixo permitido 'test $(' — cmd: test $(grep -cE 'export function MultiEscolha' apps/web/src/components/fields.tsx) -eq 1 && echo ok
  231-formulario-de-persona-guiado.md:criterios_aceite[4] ISENCAO '$(' no segmento 1 pelo prefixo permitido 'test $(' — cmd: test $(grep -oE '(MultiEscolha|FAIXAS_RENDA|TONS_DE_VOZ)' apps/web/src/app/personas/PersonaForm.tsx | wc -l) -ge 4 && echo ok
  231-formulario-de-persona-guiado.md:criterios_aceite[5] ISENCAO '$(' no segmento 1 pelo prefixo permitido 'test $(' — cmd: test $(grep -oE '(nicho_id|Outros)' apps/web/src/app/personas/PersonaForm.tsx | wc -l) -ge 3 && echo ok
  231-formulario-de-persona-guiado.md:criterios_aceite[6] ISENCAO '$(' no segmento 1 pelo prefixo permitido 'test $(' — cmd: test $(grep -c 'listarNichos' apps/web/src/app/personas/page.tsx) -ge 1 && echo ok
  231-formulario-de-persona-guiado.md:criterios_aceite[7] padrão proibido '$(' em segmento 2 ('test $(grep -c 'isPublicPath' apps/web/test/personas-form-guiado.test.tsx) -ge 1') — cmd: test -f apps/web/test/personas-form-guiado.test.tsx && test $(grep -c 'isPublicPath' apps/web/test/personas-form-guiado.test.tsx) -ge 1 && echo ok
  231-formulario-de-persona-guiado.md:criterios_aceite[10] ISENCAO '$(' no segmento 1 pelo prefixo permitido 'test $(' — cmd: test $(grep -oE 'export (function|const) (campoBase|LabelComTip|Campo|TextField|TextArea|SelectField|ListField|SubmitButton|Feedback)' apps/web/src/components/fields.tsx | wc -l) -ge 9 && echo ok
232-nome-do-nicho-na-listagem-e-na-edicao.md  2 violação(ões)
  232-nome-do-nicho-na-listagem-e-na-edicao.md:criterios_aceite[1] padrão proibido '$(' em segmento 2 ('test $(grep -c 'it(' apps/web/test/personas-nicho-nome.test.tsx) -ge 7') — cmd: test -f apps/web/test/personas-nicho-nome.test.tsx && test $(grep -c 'it(' apps/web/test/personas-nicho-nome.test.tsx) -ge 7 && echo ok
  232-nome-do-nicho-na-listagem-e-na-edicao.md:criterios_aceite[2] padrão proibido '$(' em segmento 2 ('test $(grep -cE 'export function rotuloNicho' apps/web/src/lib/nicho-exibicao.ts) -eq 1') — cmd: test -f apps/web/src/lib/nicho-exibicao.ts && test $(grep -cE 'export function rotuloNicho' apps/web/src/lib/nicho-exibicao.ts) -eq 1 && echo ok
  232-nome-do-nicho-na-listagem-e-na-edicao.md:criterios_aceite[3] ISENCAO '$(' no segmento 1 pelo prefixo permitido 'test $(' — cmd: test $(grep -o 'rotuloNicho' apps/web/src/app/personas/page.tsx | wc -l) -ge 4 && echo ok
  232-nome-do-nicho-na-listagem-e-na-edicao.md:criterios_aceite[4] ISENCAO '$(' no segmento 1 pelo prefixo permitido 'test $(' — cmd: test $(grep -c 'rotuloNicho' apps/web/src/app/personas/PersonaForm.tsx) -ge 1 && echo ok
235-servidor-de-sugestoes-por-nicho.md  6 violação(ões)
  235-servidor-de-sugestoes-por-nicho.md:criterios_aceite[1] padrão proibido '$(' em segmento 2 ('test $(grep -c 'it(' apps/web/test/sugestoes.test.ts) -ge 9') — cmd: test -f apps/web/test/sugestoes.test.ts && test $(grep -c 'it(' apps/web/test/sugestoes.test.ts) -ge 9 && echo ok
  235-servidor-de-sugestoes-por-nicho.md:criterios_aceite[2] padrão proibido '$(' em segmento 2 ('test $(grep -cE 'export async function (listarSugestoes|registrarUsoSugestao)' apps/web/src/server/sugestoes.ts) -eq 2') — cmd: test -f apps/web/src/server/sugestoes.ts && test $(grep -cE 'export async function (listarSugestoes|registrarUsoSugestao)' apps/web/src/server/sugestoes.ts) -eq 2 && echo ok
  235-servidor-de-sugestoes-por-nicho.md:criterios_aceite[3] padrão proibido '$(' em segmento 2 ('test $(grep -c 'sugestoes_persona' apps/web/src/server/sugestoes.ts) -ge 1') — cmd: test -f apps/web/src/server/sugestoes.ts && test $(grep -c 'sugestoes_persona' apps/web/src/server/sugestoes.ts) -ge 1 && echo ok
  235-servidor-de-sugestoes-por-nicho.md:criterios_aceite[4] padrão proibido '$(' em segmento 2 ('test $(grep -c 'resolverOrgAtual' apps/web/src/server/sugestoes.ts) -ge 1') — cmd: test -f apps/web/src/server/sugestoes.ts && test $(grep -c 'resolverOrgAtual' apps/web/src/server/sugestoes.ts) -ge 1 && echo ok
  235-servidor-de-sugestoes-por-nicho.md:criterios_aceite[5] ISENCAO '$(' no segmento 1 pelo prefixo permitido 'test $(' — cmd: test $(grep -oE '(sugestao_usada|marcarSugestaoUsada)' apps/web/src/server/funil.ts | wc -l) -ge 3 && echo ok
  235-servidor-de-sugestoes-por-nicho.md:criterios_aceite[6] padrão proibido '$(' em segmento 2 ('test $(grep -cE "export type TipoSugestao|export interface Sugestao" apps/web/src/server/sugestoes-types.ts) -ge 2') — cmd: test -f apps/web/src/server/sugestoes-types.ts && test $(grep -cE "export type TipoSugestao|export interface Sugestao" apps/web/src/server/sugestoes-types.ts) -ge 2 && echo ok
  235-servidor-de-sugestoes-por-nicho.md:criterios_aceite[10] ISENCAO '$(' no segmento 1 pelo prefixo permitido 'test $(' — cmd: test $(grep -oE 'export function marcar(OnboardingIniciado|ClienteCriado|AnguloCriado|PrimeiroRoteiro)' apps/web/src/server/funil.ts | wc -l) -eq 4 && echo ok
  235-servidor-de-sugestoes-por-nicho.md:criterios_aceite[11] ISENCAO '$(' no segmento 1 pelo prefixo permitido 'test $(' — cmd: test $(grep -c 'it(' apps/web/test/funil-eventos.test.ts) -ge 3 && test $(grep -c 'expect(' apps/web/test/funil-eventos.test.ts) -ge 4 && echo ok
  235-servidor-de-sugestoes-por-nicho.md:criterios_aceite[11] padrão proibido '$(' em segmento 2 ('test $(grep -c 'expect(' apps/web/test/funil-eventos.test.ts) -ge 4') — cmd: test $(grep -c 'it(' apps/web/test/funil-eventos.test.ts) -ge 3 && test $(grep -c 'expect(' apps/web/test/funil-eventos.test.ts) -ge 4 && echo ok
236-chips-de-sugestao-no-formulario-de-persona.md  2 violação(ões)
  236-chips-de-sugestao-no-formulario-de-persona.md:criterios_aceite[1] padrão proibido '$(' em segmento 2 ('test $(grep -c 'it(' apps/web/test/personas-chips-sugestao.test.tsx) -ge 9') — cmd: test -f apps/web/test/personas-chips-sugestao.test.tsx && test $(grep -c 'it(' apps/web/test/personas-chips-sugestao.test.tsx) -ge 9 && echo ok
  236-chips-de-sugestao-no-formulario-de-persona.md:criterios_aceite[2] ISENCAO '$(' no segmento 1 pelo prefixo permitido 'test $(' — cmd: test $(grep -cE '^export function ChipsSugestao' apps/web/src/components/fields.tsx) -eq 1 && echo ok
  236-chips-de-sugestao-no-formulario-de-persona.md:criterios_aceite[3] ISENCAO '$(' no segmento 1 pelo prefixo permitido 'test $(' — cmd: test $(grep -oE 'sugestoes\?:|aoUsarSugestao\?:' apps/web/src/components/fields.tsx | wc -l) -ge 2 && echo ok
  236-chips-de-sugestao-no-formulario-de-persona.md:criterios_aceite[4] ISENCAO '$(' no segmento 1 pelo prefixo permitido 'test $(' — cmd: test $(grep -c 'listarSugestoes' apps/web/src/app/personas/PersonaForm.tsx) -ge 1 && echo ok
  236-chips-de-sugestao-no-formulario-de-persona.md:criterios_aceite[5] ISENCAO '$(' no segmento 1 pelo prefixo permitido 'test $(' — cmd: test $(grep -c 'registrarUsoSugestao' apps/web/src/app/personas/PersonaForm.tsx) -ge 1 && echo ok
  236-chips-de-sugestao-no-formulario-de-persona.md:criterios_aceite[6] ISENCAO '$(' no segmento 1 pelo prefixo permitido 'test $(' — cmd: test $(grep -oE 'sugestoes=' apps/web/src/app/personas/PersonaForm.tsx | wc -l) -ge 2 && echo ok
  236-chips-de-sugestao-no-formulario-de-persona.md:criterios_aceite[7] ISENCAO '$(' no segmento 1 pelo prefixo permitido 'test $(' — cmd: test $(grep -oE 'export function (LabelComTip|Campo|TextField|TextArea|SelectField|ListField|SubmitButton|Feedback)' apps/web/src/components/fields.tsx | wc -l) -eq 8 && echo ok
  236-chips-de-sugestao-no-formulario-de-persona.md:criterios_aceite[10] ISENCAO '$(' no segmento 1 pelo prefixo permitido 'test $(' — cmd: test $(grep -c 'it(' apps/web/test/personas-form.test.tsx) -ge 2 && test $(grep -c 'expect(' apps/web/test/personas-form.test.tsx) -ge 12 && echo ok
  236-chips-de-sugestao-no-formulario-de-persona.md:criterios_aceite[10] padrão proibido '$(' em segmento 2 ('test $(grep -c 'expect(' apps/web/test/personas-form.test.tsx) -ge 12') — cmd: test $(grep -c 'it(' apps/web/test/personas-form.test.tsx) -ge 2 && test $(grep -c 'expect(' apps/web/test/personas-form.test.tsx) -ge 12 && echo ok
242-leitura-e-decisao-por-token.md  7 violação(ões)
  242-leitura-e-decisao-por-token.md:criterios_aceite[1] padrão proibido '$(' em segmento 2 ('test $(grep -c 'it(' apps/web/test/aprovacoes-publico.test.ts) -ge 10') — cmd: test -f apps/web/test/aprovacoes-publico.test.ts && test $(grep -c 'it(' apps/web/test/aprovacoes-publico.test.ts) -ge 10 && echo ok
  242-leitura-e-decisao-por-token.md:criterios_aceite[2] padrão proibido '$(' em segmento 2 ('test $(grep -cE 'export async function (lerRoteiroPorToken|decidirPorToken)' apps/web/src/server/aprovacoes-publico.ts) -eq 2') — cmd: test -f apps/web/src/server/aprovacoes-publico.ts && test $(grep -cE 'export async function (lerRoteiroPorToken|decidirPorToken)' apps/web/src/server/aprovacoes-publico.ts) -eq 2 && echo ok
  242-leitura-e-decisao-por-token.md:criterios_aceite[3] padrão proibido '$(' em segmento 2 ('test $(grep -c 'createSupabaseClientFromEnv' apps/web/src/server/aprovacoes-publico.ts) -ge 1') — cmd: test -f apps/web/src/server/aprovacoes-publico.ts && test $(grep -c 'createSupabaseClientFromEnv' apps/web/src/server/aprovacoes-publico.ts) -ge 1 && echo ok
  242-leitura-e-decisao-por-token.md:criterios_aceite[4] padrão proibido '$(' em segmento 2 ('test $(grep -oE "eq[(]'token'" apps/web/src/server/aprovacoes-publico.ts | wc -l) -ge 1') — cmd: test -f apps/web/src/server/aprovacoes-publico.ts && test $(grep -oE "eq[(]'token'" apps/web/src/server/aprovacoes-publico.ts | wc -l) -ge 1 && echo ok
  242-leitura-e-decisao-por-token.md:criterios_aceite[5] padrão proibido '$(' em segmento 2 ('test $(grep -oE 'export interface RoteiroParaCliente|export type DecisaoCliente|export interface ResultadoDecisao' apps/web/src/server/aprovacoes-types.ts | wc -l) -eq 3') — cmd: test -f apps/web/src/server/aprovacoes-types.ts && test $(grep -oE 'export interface RoteiroParaCliente|export type DecisaoCliente|export interface ResultadoDecisao' apps/web/src/server/aprovacoes-types.ts | wc -l) -eq 3 && echo ok
  242-leitura-e-decisao-por-token.md:criterios_aceite[9] padrão proibido '$(' em segmento 2 ('test $(grep -c '[.]update[(]' apps/web/src/server/aprovacoes-publico.ts) -le 1') — cmd: test -f apps/web/src/server/aprovacoes-publico.ts && test $(grep -c '[.]update[(]' apps/web/src/server/aprovacoes-publico.ts) -le 1 && echo ok
  242-leitura-e-decisao-por-token.md:criterios_aceite[11] ISENCAO '$(' no segmento 1 pelo prefixo permitido 'test $(' — cmd: test $(grep -oE 'export interface (PacoteEntrega|LegendaPersistida|ResultadoPersistencia|LinkAprovacao|ResultadoLink)' apps/web/src/server/aprovacoes-types.ts | wc -l) -eq 5 && echo ok
  242-leitura-e-decisao-por-token.md:criterios_aceite[12] ISENCAO '$(' no segmento 1 pelo prefixo permitido 'test $(' — cmd: test $(grep -c 'it(' apps/web/test/aprovacoes-pacote.test.ts) -ge 7 && test $(grep -c 'it(' apps/web/test/aprovacoes-link.test.ts) -ge 8 && echo ok
  242-leitura-e-decisao-por-token.md:criterios_aceite[12] padrão proibido '$(' em segmento 2 ('test $(grep -c 'it(' apps/web/test/aprovacoes-link.test.ts) -ge 8') — cmd: test $(grep -c 'it(' apps/web/test/aprovacoes-pacote.test.ts) -ge 7 && test $(grep -c 'it(' apps/web/test/aprovacoes-link.test.ts) -ge 8 && echo ok
243-rota-e-tela-publica-de-aprovacao.md  6 violação(ões)
  243-rota-e-tela-publica-de-aprovacao.md:criterios_aceite[1] padrão proibido '$(' em segmento 2 ('test $(grep -c 'it(' apps/web/test/aprovar-publico.test.tsx) -ge 6') — cmd: test -f apps/web/test/aprovar-publico.test.tsx && test $(grep -c 'it(' apps/web/test/aprovar-publico.test.tsx) -ge 6 && echo ok
  243-rota-e-tela-publica-de-aprovacao.md:criterios_aceite[2] padrão proibido '$(' em segmento 2 ('test $(grep -oE 'updateSession|@supabase/ssr' apps/web/test/aprovar-publico.test.tsx | wc -l) -ge 2') — cmd: test -f apps/web/test/aprovar-publico.test.tsx && test $(grep -oE 'updateSession|@supabase/ssr' apps/web/test/aprovar-publico.test.tsx | wc -l) -ge 2 && echo ok
  243-rota-e-tela-publica-de-aprovacao.md:criterios_aceite[4] ISENCAO '$(' no segmento 1 pelo prefixo permitido 'test $(' — cmd: test $(grep -cE "'/aprovar'" apps/web/src/lib/auth/routes.ts) -eq 1 && echo ok
  243-rota-e-tela-publica-de-aprovacao.md:criterios_aceite[5] padrão proibido '$(' em segmento 2 ('test $(grep -oE 'lerRoteiroPorToken|notFound' 'apps/web/src/app/aprovar/[token]/page.tsx' | wc -l) -ge 3') — cmd: test -f 'apps/web/src/app/aprovar/[token]/page.tsx' && test $(grep -oE 'lerRoteiroPorToken|notFound' 'apps/web/src/app/aprovar/[token]/page.tsx' | wc -l) -ge 3 && echo ok
  243-rota-e-tela-publica-de-aprovacao.md:criterios_aceite[6] padrão proibido '$(' em segmento 2 ('test $(grep -c 'decidirPorToken' 'apps/web/src/app/aprovar/[token]/page.tsx') -ge 1') — cmd: test -f 'apps/web/src/app/aprovar/[token]/page.tsx' && test $(grep -c 'decidirPorToken' 'apps/web/src/app/aprovar/[token]/page.tsx') -ge 1 && echo ok
  243-rota-e-tela-publica-de-aprovacao.md:criterios_aceite[7] ISENCAO '$(' no segmento 1 pelo prefixo permitido 'test $(' — cmd: test $(grep -oE "/aprovar|/aprovacoes" apps/web/test/auth-validation.test.ts | wc -l) -ge 4 && echo ok
  243-rota-e-tela-publica-de-aprovacao.md:criterios_aceite[8] ISENCAO '$(' no segmento 1 pelo prefixo permitido 'test $(' — cmd: test $(grep -oE "'/[a-z-]+'" apps/web/src/lib/auth/routes.ts | wc -l) -eq 5 && echo ok
  243-rota-e-tela-publica-de-aprovacao.md:criterios_aceite[11] ISENCAO '$(' no segmento 1 pelo prefixo permitido 'test $(' — cmd: test $(grep -c 'it(' apps/web/test/auth-validation.test.ts) -ge 9 && test $(grep -c 'expect(' apps/web/test/auth-validation.test.ts) -ge 40 && echo ok
  243-rota-e-tela-publica-de-aprovacao.md:criterios_aceite[11] padrão proibido '$(' em segmento 2 ('test $(grep -c 'expect(' apps/web/test/auth-validation.test.ts) -ge 40') — cmd: test $(grep -c 'it(' apps/web/test/auth-validation.test.ts) -ge 9 && test $(grep -c 'expect(' apps/web/test/auth-validation.test.ts) -ge 40 && echo ok
  243-rota-e-tela-publica-de-aprovacao.md:criterios_aceite[13] ISENCAO '$(' no segmento 1 pelo prefixo permitido 'test $(' — cmd: test $(grep -c 'it(' apps/web/test/briefing-gate.test.ts) -ge 9 && test $(grep -c 'expect(' apps/web/test/briefing-gate.test.ts) -ge 16 && echo ok
  243-rota-e-tela-publica-de-aprovacao.md:criterios_aceite[13] padrão proibido '$(' em segmento 2 ('test $(grep -c 'expect(' apps/web/test/briefing-gate.test.ts) -ge 16') — cmd: test $(grep -c 'it(' apps/web/test/briefing-gate.test.ts) -ge 9 && test $(grep -c 'expect(' apps/web/test/briefing-gate.test.ts) -ge 16 && echo ok
244-fila-interna-de-aprovacao.md  7 violação(ões)
  244-fila-interna-de-aprovacao.md:criterios_aceite[1] padrão proibido '$(' em segmento 2 ('test $(grep -c 'it(' apps/web/test/aprovacoes-fila.test.tsx) -ge 9') — cmd: test -f apps/web/test/aprovacoes-fila.test.tsx && test $(grep -c 'it(' apps/web/test/aprovacoes-fila.test.tsx) -ge 9 && echo ok
  244-fila-interna-de-aprovacao.md:criterios_aceite[2] padrão proibido '$(' em segmento 2 ('test $(grep -cE 'export async function listarFilaDeAprovacao' apps/web/src/server/aprovacoes-read.ts) -eq 1') — cmd: test -f apps/web/src/server/aprovacoes-read.ts && test $(grep -cE 'export async function listarFilaDeAprovacao' apps/web/src/server/aprovacoes-read.ts) -eq 1 && echo ok
  244-fila-interna-de-aprovacao.md:criterios_aceite[3] padrão proibido '$(' em segmento 2 ('test $(grep -oE "'(sem_link|aguardando|aprovado|ajuste_pedido|revogado|expirado)'" apps/web/src/server/aprovacoes-read.ts | wc -l) -ge 6') — cmd: test -f apps/web/src/server/aprovacoes-read.ts && test $(grep -oE "'(sem_link|aguardando|aprovado|ajuste_pedido|revogado|expirado)'" apps/web/src/server/aprovacoes-read.ts | wc -l) -ge 6 && echo ok
  244-fila-interna-de-aprovacao.md:criterios_aceite[5] padrão proibido '$(' em segmento 2 ('test $(grep -oE 'gerarLinkAprovacao|revogarLinkAprovacao' apps/web/src/app/aprovacoes/page.tsx | wc -l) -ge 2') — cmd: test -f apps/web/src/app/aprovacoes/page.tsx && test $(grep -oE 'gerarLinkAprovacao|revogarLinkAprovacao' apps/web/src/app/aprovacoes/page.tsx | wc -l) -ge 2 && echo ok
  244-fila-interna-de-aprovacao.md:criterios_aceite[6] padrão proibido '$(' em segmento 2 ('test $(grep -oE 'roteiro_aprovacoes|roteiros' apps/web/src/server/aprovacoes-read.ts | wc -l) -ge 2') — cmd: test -f apps/web/src/server/aprovacoes-read.ts && test $(grep -oE 'roteiro_aprovacoes|roteiros' apps/web/src/server/aprovacoes-read.ts | wc -l) -ge 2 && echo ok
  244-fila-interna-de-aprovacao.md:criterios_aceite[13] ISENCAO '$(' no segmento 1 pelo prefixo permitido 'test $(' — cmd: test $(grep -c 'it(' apps/web/test/aprovacoes-link.test.ts) -ge 8 && test $(grep -c 'it(' apps/web/test/aprovacoes-publico.test.ts) -ge 10 && test $(grep -c 'it(' apps/web/test/aprovar-publico.test.tsx) -ge 6 && echo ok
  244-fila-interna-de-aprovacao.md:criterios_aceite[13] padrão proibido '$(' em segmento 2 ('test $(grep -c 'it(' apps/web/test/aprovacoes-publico.test.ts) -ge 10') — cmd: test $(grep -c 'it(' apps/web/test/aprovacoes-link.test.ts) -ge 8 && test $(grep -c 'it(' apps/web/test/aprovacoes-publico.test.ts) -ge 10 && test $(grep -c 'it(' apps/web/test/aprovar-publico.test.tsx) -ge 6 && echo ok
  244-fila-interna-de-aprovacao.md:criterios_aceite[13] padrão proibido '$(' em segmento 3 ('test $(grep -c 'it(' apps/web/test/aprovar-publico.test.tsx) -ge 6') — cmd: test $(grep -c 'it(' apps/web/test/aprovacoes-link.test.ts) -ge 8 && test $(grep -c 'it(' apps/web/test/aprovacoes-publico.test.ts) -ge 10 && test $(grep -c 'it(' apps/web/test/aprovar-publico.test.tsx) -ge 6 && echo ok

TOTAL: 33 violação(ões), 25 isenção(ões), em 7 ticket(s)
```


## Etapa 3, sessão B — fila de trabalho

1. ~~**O juiz (`avaliador`) — o papel que falta.**~~ **FEITO em 2026-09-03** (peça 1 acima); o desenho abaixo foi seguido item a item e fica como registro do que foi combinado antes de existir. Hoje não existe: `avaliador_model` está no config e entra no probe, mas nenhum código o consome, e critério com `espera: "avaliador"` é PULADO em `executor.sh:308`. Todo julgamento é mecânico, e a auditoria do 204 acima mostra o preço disso. Desenho fechado:
   - **Entrada:** diff cru da tentativa + outputs dos gates + os critérios de aceite, **inclusive os de tipo `avaliador`** — que hoje não chegam a lugar nenhum.
   - **Saída:** JSON `{aprovado, motivo, criterios_falhos}`. `criterios_falhos` alimenta o campo que o `decisao.ts` já entende, então a fronteira adiar/reprovar não muda de dono.
   - **Chamada:** `--output-format json` + `custo_registrar juiz <id> <attempt>`, como as outras duas. **O teste que hoje fixa 2 chamadas passa a fixar 3** (`test/orquestrador-custo.test.ts`, `as duas chamadas (agente e sondagem) usam json`) — é essa asserção que impede o custo do juiz de escapar do gate de orçamento, o único freio de gasto do loop.
   - **Falha de parse do veredito = ADIADO**, nunca reprovado: veredito ilegível não é veredito sobre o trabalho, e reprovar por ele queima retry de um ticket não julgado — a mesma regra do envelope ausente da peça 9.
   - **Output cru preservado** em `runs/<id>/attempt-N/`, ao lado de `prompt.txt`, `claude.txt` e `veredito.json`. Sem isso não há como auditar o juiz depois, que é o buraco que esta sessão acabou de pagar à mão.
   - **Nível pelo risco:** `modelos.juiz_baixo` (sonnet) e `modelos.juiz_alto` (opus), já no config. Julgar tudo em opus é gasto sem hipótese; julgar tudo em sonnet é o juiz virar carimbo.
